# Environment and local secrets
.env
.env.*
!.env.example

# Go build output
/wildtoken
/wildtoken.exe
# `go build` inside cmd/ leaves the binary beside its package rather than at the
# repository root, so the root-anchored patterns above do not catch it.
cmd/wildtoken
cmd/wildtoken.exe
.wildtoken.bin

# Local AI tool state
.opencode/
.slim/
.agents/
.codex/
.claude/

# Runtime data and logs
*.log
*.pid
*.db
*.db-shm
*.db-wal
wildtoken.db*
*.sqlite
*.sqlite3
data/

# Editors and operating systems
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Build output and local design references
/target/
/渠道卡片/
