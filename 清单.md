# WildToken 功能优化与迭代规划清单

> 记录 WildToken 已完成能力、现存正确性问题与后续迭代计划。本文于 2026-08-18 结合当前工作区代码完成状态校准。
>
> 状态约定：`[x]` 表示代码和主要交互已落地；`[ ]` 表示仍需实施。部分完成的能力拆成独立子项，不再用一个总勾选掩盖缺口。

---

## 📌 规划总览与优先级矩阵

| 优先级 | 模块方向 | 核心目标 | 预估收益 |
| :--- | :--- | :--- | :--- |
| **P0-A** | 现有功能正确性 | 修复错误下钻、范围切换竞态、健康统计与令牌精确过滤 | 极高（避免排障数据误导） |
| **P0-B** | 运维排障 & 下钻体验 | 时间范围联动、批量测活、日志高级过滤 | 极高（日常运维核心痛点） |
| **P1** | 网关调度 & 高可用 | 校准重试策略、流式首字前故障转移、最低延迟调度 | 极高（网关核心高可用） |
| **P2** | 配额管控 | 令牌模型白名单、周期性配额重置（模型单价与成本折算已作废移除） | 高（企业/团队多租户预算管理） |
| **P3** | 监控生态 & 灾备迁移 | Prometheus `/metrics`、配置迁移、SQLite 一致性备份 | 中高（生产级部署与运维规范） |

---

## ✅ 已完成功能清单 (Completed)

### 1. 数据看板 (Dashboard) 主体能力（部分完成）

- [x] **统计说明与文案修正**：流量指标、趋势与排行按所选周期统计；渠道启用状态等运行态数据属于当前快照。
- [x] **指标语义 Tooltip**：
  - [x] P50 / P95 / P99 延迟分位数详细业务含义解释。
  - [x] Prompt 缓存命中率计算公式与成本节省价值说明。
- [x] **Top 客户端令牌排行榜**：
  - [x] Top 令牌请求数排行榜（含平均耗时与错误率元信息）。
  - [x] Top 令牌消耗量排行榜（Tokens 维度）。
- [x] **基础下钻交互 (Drill-Down)**：
  - [x] Top 渠道按渠道 ID 精确跳转并过滤日志。
  - [x] Top 模型 / Top 令牌可跳转并填充搜索关键词。
  - [x] 状态分布支持 `2xx` / `4xx` / `5xx` 基础过滤。
  - [x] 支持键盘 Enter / Space 键无障碍触发下钻。
- [x] **中文状态码错误归因**：状态分布与最近失败列表提供详细语义提示（429 限流/配额、401/403 鉴权失败、504 超时、502 坏网关等）。
- [x] **看板自动刷新控制**：支持 5s / 15s / 30s / 暂停刷新切换，偏好保存在 `localStorage`。
- [x] **宽屏响应式排版**：`>=1400px` 宽屏下排行榜自适应为 3 列 × 2 行网格。

> “全维度一键穿透”不再标记为完整完成：错误时间条、`other` 状态桶、令牌 ID 精确过滤及时间范围继承仍在 P0-A/P0-B 中。

### 2. 渠道模型别名映射与重定向

- [x] 渠道持久化 `model_mappings` 配置。
- [x] 映射键参与模型匹配和渠道选择。
- [x] 转发前重写请求 Body 顶层 `model` 字段。
- [x] 管理台支持模型映射配置与展示。

### 3. 已有调度与健康基础

- [x] Priority 主备调度：仅在高优先级层无可用权重时降级。
- [x] 固定权重随机调度。
- [x] 基础权重 × Runtime Health Score 的动态有效权重。
- [x] 单渠道测试接口与控制台入口。
- [x] 基于近 24 小时请求日志的流量健康概览。

### 4. 已有日志与 Usage 基础

- [x] 请求日志记录 `stream`、`duration_ms`、`first_token_ms`。
- [x] 保存受大小与保留策略限制的上游/下游响应快照，可展示错误 Body 摘要。
- [x] 记录 Prompt、Completion、Cache Read、Cache Create、Reasoning 等 Usage 维度。

---

## 🔴 P0-A：先修复现有功能正确性

### A1. 对齐错误统计、状态分桶与下钻语义

- [x] 为日志过滤增加统一的“全部错误”语义，不能把所有非 2xx 错误固定下钻为 `4xx`。
- [x] 最近失败列表、错误率、错误时间条和状态下钻共享同一“全部错误”语义；若产品不展示 1xx/3xx，必须同步收窄总错误统计定义。
- [x] 统一看板与日志的 `2xx` / `4xx` / `5xx` / `other` / `none` 定义。
- [x] `other` 单独覆盖 1xx、3xx 等状态；`none` 仅代表无 HTTP 状态。
- [x] 点击错误时间格时同时携带当前看板范围；如需精确到格子，应携带该时间桶的 `start` / `end`。
- [x] 增加状态桶、错误条和时间桶的 API/DOM 行为测试。
- [x] **涉及模块**：`internal/db/logoverview.go`、`internal/db/log.go`、`internal/handlers/admin.go`、`static/js/dashboard.js`、`static/js/logs.js`、`tests/dashboard-enhancements.test.mjs`。

**完成标准**：任一状态桶的展示数量、Tooltip 文案和点击后的日志集合使用同一查询口径。

### A2. 保证范围切换后的最后一次加载一定生效

- [x] 使用请求 generation、`AbortController` 或 queued reload，替代“加载中直接丢弃新请求”。
- [x] 快速连续切换预设/自定义范围时，以最后一次选择为准。
- [x] 暂停自动刷新后，也必须完成用户最后一次主动范围加载。
- [x] 增加慢请求与连续切换的竞态测试。
- [x] **涉及模块**：`static/js/dashboard.js`、`static/js/events.js`、`static/js/shell.js`、`tests/dashboard-time-range.test.mjs`。

**完成标准**：无论旧请求以何种顺序返回，最终控件、持久化状态和看板数据均对应最后一次选择。

### A3. 修正渠道健康统计口径

- [x] 客户端主动断开产生的 499 不计为渠道故障。
- [x] 平均延迟按实际存在合法耗时的 `timed_count` 加权，不能用所有请求数作为分母。
- [x] 合法 0ms 样本不得被过滤。
- [x] UI 明确区分“24h 流量健康”和“最近一次主动探针结果”。
- [x] **涉及模块**：`internal/db/upstreamhealth.go`、`internal/proxy/ssestream.go`、`static/js/upstreams.js` 及对应 Go/Node 测试。

**完成标准**：客户端取消不降低渠道在线率；跨时间桶的平均耗时与原始有效样本重新聚合结果一致。

### A4. 令牌下钻改用稳定 ID

- [x] `LogFilter` 和管理 API 增加 `downstream_token_id` 精确过滤。
- [x] Top 令牌下钻优先传递 ID，不依赖可能为空、重名或后续变更的显示名称。
- [x] 实时日志客户端过滤同步支持令牌 ID。
- [x] **涉及模块**：`internal/db/log.go`、`internal/handlers/admin.go`、`static/js/dashboard.js`、`static/js/events.js`、`static/js/logs.js`。

**完成标准**：空名称、同名及改名令牌都只能下钻到自己的历史和实时日志。

---

## 🎯 待办迭代清单 (Roadmap & Backlog)

### 🚀 【P0-B 级】核心运维与下钻体验

#### 1. 看板下钻时间范围同步联动 (Time Range Synchronous Drill-Down)

- [x] **目标**：从看板点击下钻跳转至「日志」视图时，继承看板当前选中的时间窗口（如「今天」、「最近 24 小时」、「自定义日期范围」）。
- [x] **技术要点**：
  - [x] `drillDownToLogs` 传递规范化的 `start` / `end`，后端 `LogFilter` 执行数据库级过滤。
  - [x] 明确边界为 `[start, end)`，统一浏览器时区、API 时间格式和 SQLite 时间比较。
  - [x] 日志控件、分页请求和导出/刷新行为共享同一范围状态。
  - [x] 查看历史自定义范围时，实时日志只接收范围内事件，或明确暂停实时流（由 `openEndedForLiveStream` 保证）。
- [x] **涉及模块**：`static/admin.html`、`static/js/dashboard.js`、`static/js/logs.js`、`static/js/events.js`、`internal/handlers/admin.go`、`internal/db/log.go` 及对应测试。

**完成标准**：预设和自定义范围在看板与历史分页日志之间保持一致，翻页、刷新和实时事件不会越界。

#### 2. 渠道一键批量测活与健康度徽标评级 (Batch Probe & Health Status)

- [x] **目标**：渠道列表顶部提供一键批量测活，卡片上直观显示健康评级与最近测活详情。
- [x] **技术要点**：
  - [x] 增加 `POST /api/admin/upstreams/probe-all`，使用有界并发、整体超时与防重复执行。
  - [x] 单项结果返回 `upstream_id`、`ok`、`status_code`、`duration_ms`、`error_summary`、`checked_at`。
  - [x] 保存或缓存最近一次主动探针结果，并与 24h 流量健康分开展示（内存缓存，理由见交接记录）。
  - [x] 批量结果允许部分成功；一个渠道超时不能阻塞或取消其他结果。
- [x] **涉及模块**：`internal/app/router.go`、`internal/handlers/upstreams.go`、`internal/db/upstream.go`、`static/admin.html`、`static/js/upstreams.js`、`static/css/enhancements.css` 及对应测试；探针结果选内存缓存，未新增 `internal/db/upstreamprobe.go`。

**完成标准**：大量渠道下批量探针不产生无界 goroutine；每张卡片能区分启用状态、流量健康和最近探针结果。

#### 3. 日志高级过滤增强与 TTFB 首字耗时拆解 (Advanced Log Filters & TTFB)

- [x] 已记录 `first_token_ms`、`duration_ms` 和 `stream`。
- [x] 已提供上游/下游响应快照及错误 Body 摘要。
- [x] 增加按客户端令牌 ID 的下拉筛选。
- [x] 增加耗时阈值筛选，如 `>3s`、`>10s`。
- [x] 增加流式 / 非流式筛选。
- [x] 先定义排队、上游首字、传输阶段的精确起止点；缺失阶段需新增采样字段，不能由前端估算（后端与前端四段瀑布、重试徽标均已全部落地）。
- [x] 日志详情增加首字/传输/总耗时可视化；没有排队行为时不得虚构“排队耗时”。
- [x] **涉及模块**：`internal/models/requestlog.go`、`internal/db/sql.go`、`internal/db/log.go`、`internal/proxy/client.go`、`internal/proxy/logging.go`、`static/admin.html`、`static/js/logs.js` 及对应测试。

**完成标准**：所有新增筛选在数据库分页、总数统计和实时日志中口径一致；瀑布各阶段可由原始时间点复算。

---

### 🛡️ 【P1 级】网关路由与高可用强化

#### 5. 流式首字前智能故障转移与自动重试 (Smart First-Token Fallback)

- [x] 已有多渠道尝试循环和基础失败切换能力。
- [x] **目标**：校准可重试错误，并在 SSE 尚未提交下游 Header/首事件前支持透明切换。
- [x] **技术要点**：
  - 只对传输错误、超时、429 和约定的 5xx 重试；3xx 与不可重试 4xx 不再无差别切换。
  - 在收到可用 SSE 首事件前暂存上游 Header 和有限前导数据；只有确认成功后才提交下游。
  - 设置首事件等待超时、最大前导缓冲和总尝试上限。
  - 一旦下游 Header 或数据已经提交，禁止再次切换渠道。
  - 请求日志记录结构化重试链路，包括渠道、耗时、状态和失败阶段。
- [x] **涉及模块**：`internal/handlers/proxy.go`、`internal/proxy/client.go`、`internal/proxy/bufferedstream.go`、`internal/proxy/ssestream.go`、`internal/models/requestlog.go`、`internal/db/sql.go` 及对应测试。

**完成标准**：2xx SSE 在首事件前超时/断流时可切换；首事件后失败不会重放；401/403 等请求错误不会跨渠道放大。

#### 6. 渠道负载均衡与调度算法扩展 (Load Balancing Strategies)

- [x] 固定权重调度。
- [x] 健康分数参与有效权重。
- [x] Priority 1/2/3 主备容灾。
- [x] **剩余目标——最低延迟优先 (Least-Latency)**：
  - 路由热路径维护有界滚动延迟数据，不能直接复用 Dashboard 的日志扫描查询。
  - 设定最小样本数、陈旧窗口、无数据回退和异常值处理。
  - 增加迟滞或权重混合，避免多个渠道因短期 P50 波动频繁抖动。
  - 与 Priority 规则组合：先选择最高可用优先级层，再在层内执行最低延迟策略。
- [x] **涉及模块**（后端部分已完成；`static/js/upstreams.js` 属前端范围）：`internal/proxy/matcher.go`、`internal/proxy/latency.go`、`internal/models/upstream.go`、`internal/models/settings.go`、`internal/db/settings.go` 及对应测试。

**完成标准**：冷启动、无样本和样本过期时稳定回退；低样本偶然值不会长期垄断流量。

---

### 💰 【P2 级】精细化配额管控

#### 7. 客户端令牌模型白名单与访问权限控制 (Token Model Whitelist)

- [x] **目标**：限制特定 API Key 只能调用指定模型列表，防止下游客户端滥用高成本大模型。（前后端全量完成）
- [x] **技术要点**：
  - 令牌数据表新增 `allowed_models` JSON 数组，空数组/NULL 的“不限制”语义必须唯一且明确。
  - 鉴权中间件只负责加载令牌策略；在 Proxy Handler 解析请求 Body 中的客户端模型后执行校验。
  - 白名单按客户端请求模型判断，并明确别名/通配符的匹配规则，不能按映射后的上游模型误判。
  - `/v1/models` 返回结果按令牌、Group 和白名单求交集；相关缓存键必须包含策略版本。
  - 拒绝时返回 OpenAI 兼容的 403 错误结构。
  - 令牌创建/编辑弹窗提供模型选择勾选框与快捷标签输入。（已落地）
- [x] **涉及模块**（后端已完成）：`internal/db/sql.go`、`internal/db/schema.go`、`internal/db/token.go`、`internal/models/token.go`、新增 `internal/models/modelpolicy.go`、`internal/middleware/auth.go`、`internal/handlers/proxy.go`、`internal/appstate/state.go` 及对应测试；`static/js/tokens.js`、`static/admin.html` 属前端范围。

**完成标准**：代理请求与模型列表使用同一授权口径；策略更新后不会继续命中旧缓存。

#### 8. 模型费率倍率与预估消费折算 (Model Pricing & Cost Multiplier) — 已于 2026-08-20 整体移除

> **该需求已作废，不再是本项目的目标。** 曾于 2026-08-19 前后端落地，2026-08-20 按运营者决定整条移除：本网关不产生账单，估算金额只是一个需要持续维护价格表才能保持正确的数字，而一个陈旧的价格表给出的是"看起来精确、实际偏差"的金额——比不显示金额更容易误导。移除详情见文末《费用估算功能移除记录》。

#### 9. 周期性配额自动重置计划 (Periodic Quota Reset Cycles)

- [x] **目标**：支持为 API Key 设置「每日 / 每周 / 每月」自动重置配额上限。（前后端全量完成）
- [x] **技术要点**：
  - 增加周期类型、时区/锚点、当前 `period_key` 或 generation、周期起止时间。
  - 不采用简单 `used_tokens = 0`：异步 Usage 日志和在途 reservation 必须绑定其发生时的周期。
  - 周期滚动、额度检查、预留和最终结算使用事务或等价的原子语义。
  - 明确每日/每周/月末、夏令时和服务停机跨周期后的行为。
- [x] **涉及模块**（前后端全量完成）：新增 `internal/models/quotaperiod.go`、`internal/db/quotaperiod.go`；并修改 `internal/models/token.go`、`internal/db/token.go`、`internal/db/sql.go`、`internal/db/schema.go`、`internal/quota/tracker.go`、`internal/middleware/auth.go`、`internal/proxy/logging.go`、`internal/proxy/client.go`、`internal/handlers/proxy.go`、`static/js/tokens.js`、`static/admin.html` 及对应测试。未改 `internal/proxy/ratelimit.go`：速率限制与配额周期无关。

**完成标准**：跨周期在途请求和晚到日志不会污染新周期，重启后周期状态仍可恢复。

---

### ⚙️ 【P3 级】监控生态与自动化运维

#### 10. Prometheus `/metrics` 监控端点集成 (Prometheus Exporter)

- [x] **目标**：暴露标准 Prometheus 格式指标，支持与企业级 Grafana 大屏和 Alertmanager 监控告警打通。
- [x] **技术要点**：
  - 暴露 `GET /metrics`，默认关闭或使用独立 Bearer Token/内网访问策略保护。
  - 输出指标项：
    - `wildtoken_http_requests_total{upstream_id,status_class,protocol}`
    - `wildtoken_http_request_duration_seconds{upstream_id,protocol}`（直方图）
    - `wildtoken_tokens_total{type="prompt|completion|cache_read|cache_create|reasoning"}`
    - `wildtoken_upstream_health_status{upstream_id}`
  - 默认禁止 `token_id`、令牌名称和任意模型字符串标签，避免隐私泄露与高基数。
  - 现有 `/api/admin/system/metrics` JSON 端点继续用于控制台，不与 Prometheus 契约混用。
- [x] **涉及模块**：建议新增 `internal/metrics/prometheus.go`、`internal/handlers/metrics.go`；并修改 `internal/app/router.go`、`internal/metrics/metrics.go`、`go.mod` 及对应测试。

**完成标准**：Prometheus 文本格式合法，敏感令牌信息不可见，标签基数在模型/令牌数量增长时仍有上界。

#### 11A. 配置迁移包 (Portable Configuration Export/Import)

- [x] 已有渠道级导入导出基础。
- [x] 导出分组、渠道、令牌策略和系统设置，并带 `schema_version`、应用版本与校验和。
- [x] 渠道与分组使用稳定名称/迁移键关联，不能复用来源实例的数字 `GroupIDs`。
- [x] 导入支持校验预览、冲突策略和 dry-run；校验失败不得产生部分写入。
- [x] 敏感配置使用带认证加密（AEAD）和带随机盐的密码派生，不允许明文落盘。

**完成标准**：在分组数字 ID 完全不同的新实例中仍可正确迁移；错误密码、篡改归档或版本不兼容会在写入前失败。

#### 11B. SQLite 一致性全量备份与恢复 (Disaster Recovery)

- [x] 使用 SQLite 一致性快照/在线备份机制生成数据库备份，不能把多张表分别导出后拼成“全量备份”。
- [x] 备份包含 schema/application 版本、生成时间、校验和和加密元数据。
- [x] 恢复前自动生成可回滚备份；恢复采用临时文件校验和原子替换，并明确是否需要重启。
- [x] 将配置迁移 API 与灾备 API 分开，分别授权和审计。
- [x] **涉及模块**：建议新增 `internal/handlers/backup.go`、`internal/db/backup.go`；并修改 `internal/app/router.go`、`static/admin.html`、`static/js/shell.js` 及对应集成测试。

**完成标准**：在持续写日志场景下生成的备份通过 `integrity_check`，并能在空白实例完成一次自动化恢复演练。

---

## 🧩 前后端实施拆分与协作契约

本节用于将本清单直接拆给两个执行者。默认建议：

- **Codex：后端执行者**，负责 Go、SQLite、API、代理、调度、配额、指标和后端测试。
- **Claude Code：前端执行者**，负责 `static/`、Dashboard、日志/渠道/令牌交互、样式和 Node 测试。
- 如果实际分工相反，只交换执行者，不改变下面的 API 契约和验收标准。

### 协作规则

1. 后端先冻结请求参数、返回字段、空值语义和错误码；前端可以用 mock 数据并行开发，但不得自行改变契约。
2. 后端负责所有核心统计、权限、重试、配额和金额计算；前端不得通过 JavaScript 重算这些业务口径来替代 API。
3. 前端负责统一维护筛选状态、范围状态、加载竞态和用户交互；后端接口必须支持这些状态在分页、刷新和实时流中保持一致。
4. 两个执行者不得同时修改同一个文件。共享文件需先由后端完成契约，再由前端接入；若必须共同修改，应分成前后两个提交。
5. 每个任务完成后，执行者必须在本节对应小项勾选，并记录实际改动文件、测试命令和未完成项。

### 统一 API 与数据语义

#### 日志状态筛选

日志 API 的 `status` 统一支持：

```text
2xx    status_code ∈ [200, 299]
4xx    status_code ∈ [400, 499]
5xx    status_code ∈ [500, 599]
other  status_code ∈ [100, 199] 或 [300, 399]
none   status_code IS NULL
error  status_code IS NULL 或 status_code < 200 或 status_code >= 300
```

`error` 是“全部错误”筛选；`other` 不等于错误，但仍属于非 2xx。Dashboard 错误率、错误时间条、最近失败列表和日志下钻必须共享上述定义。

#### 日志时间范围

历史日志接口统一使用：

```text
start=<RFC3339 时间>
end=<RFC3339 时间>
```

语义为 `[start, end)`，`start` 与 `end` 必须同时存在，且 `start < end`。后端负责解析、校验、时区转换和数据库过滤；前端只保存规范化值并在分页、刷新、导出和实时流请求中复用。

#### 日志筛选状态

前端统一维护以下状态，不得只修改控件而遗漏实际请求参数：

```js
{
  upstreamId: "",
  downstreamTokenId: "",
  search: "",
  status: "",
  clientType: "",
  stream: "",
  minDurationMs: "",
  start: "",
  end: ""
}
```

后端 `ListLogs`、日志总数/分页、实时日志和导出接口应尽量复用同一组筛选语义。

---

### P0-A1：错误统计、状态分桶与下钻语义

**Codex（后端）**

- [x] 扩展 `internal/db/log.go` 的 `LogFilter`，支持 `other`、`error` 和统一状态 SQL。
- [x] 更新 `internal/handlers/admin.go` 的状态参数解析，明确非法值处理方式（非法值返回 400）。
- [x] 让 `internal/db/logoverview.go` 的错误总数、状态桶、错误时间桶与 `status=error` 使用同一口径。
- [x] 时间桶返回 `bucket_epoch`、`bucket_seconds`、`count`、`errors`（已有字段满足；未新增 `start` / `end`，前端可由 `bucket_epoch + bucket_seconds` 构造 `[start,end)`）。
- [x] 增加 DB、handler 和聚合测试，覆盖 200、302、404、499、502、NULL。

**Claude Code（前端）**

- [x] `static/js/dashboard.js` 的错误时间格改为传 `status=error`，不得固定为 `4xx`（`data-drill-status="error"`）。
- [x] `other`、`none`、`error` 的图例、Tooltip、空态和下钻参数保持一致。
- [x] 最近失败列表使用后端统一的错误筛选，不再只用 `null` 或 `>=400` 做前端二次判断。
- [x] 时间桶下钻携带当前看板范围及该桶的 `[start, end)`。
- [x] 在 `tests/dashboard-enhancements.test.mjs` 增加状态桶、错误格和下钻参数断言。

**依赖与验收**

- [x] 先冻结 `status` 枚举，再进行前端接入。
- [x] 任一状态桶的数量、文案、失败列表和点击后的日志集合一致。

### P0-A2：范围切换加载竞态

**Codex（后端）**

- [x] 确认 Dashboard 各接口使用 `r.Context()`，客户端取消后数据库查询可结束（overview/top/token-usage/logs 均已使用请求 Context）。
- [x] 对明显的聚合慢查询增加合理超时或可观测日志，不改变业务结果（`ListLogs` 保留 `LogQueryTimeout`，派生自请求 Context）。
- [x] 增加 handler/查询取消测试：覆盖 `ListLogs` 运行中取消、详情查询、Dashboard 聚合端点、连接归还与超时边界；SQLite 驱动在本机实测将 `context.Canceled` / `DeadlineExceeded` 原样包入错误链。

**Claude Code（前端）**

- [x] 在 `static/js/dashboard.js` / `static/js/bootstrap.js` 引入 generation、`AbortController` 或等价 queued reload。
- [x] 用户主动切换范围必须取消或淘汰旧请求；自动刷新不得覆盖最后一次用户选择。
- [x] 只有最新 generation 能更新 DOM、缓存、持久化状态和错误提示。
- [x] Abort 产生的旧请求错误不得显示为看板失败。
- [x] 在 `tests/dashboard-time-range.test.mjs` 增加慢 A/快 B、乱序返回、暂停刷新和手动加载测试。

**验收**

- [x] 任意返回顺序下，最终界面都对应最后一次范围选择。

### P0-A3：渠道健康统计口径

**Codex（后端）**

- [x] 修正 `internal/db/upstreamhealth.go`：499 不计渠道故障。
- [x] 返回或内部计算 `timed_count`；使用 `timed_count` 而不是总请求数作为延迟均值分母。
- [x] 合法 `0ms` 样本必须计入；不得用 `avg_ms > 0` 判断是否有样本。
- [x] 将 24h 流量健康与主动探针结果定义为不同数据源（P0-B2 已实施；健康统计另排除探针 `client_type`）。
- [x] 增加 499、0ms、NULL duration 和跨桶加权平均测试。

**Claude Code（前端）**

- [x] 在 `static/js/upstreams.js` 中分开显示“24h 流量健康”和“最近主动探针”。
- [x] 处理 `timed_count=0`、`avg_ms=0`、无样本和 499 文案。
- [x] 不把客户端主动断开渲染为渠道故障红色状态。
- [x] 增加渠道健康卡片和边界数据的 Node 测试。

**验收**

- [x] 客户端取消不降低渠道健康度；跨桶平均延迟等于按有效耗时样本重新聚合的结果。

### P0-A4：Token 稳定 ID 下钻

**Codex（后端）**

- [x] 在 `internal/db/log.go` 的 `LogFilter` 增加 `DownstreamTokenID *int64`。
- [x] 在 `internal/handlers/admin.go` 解析 `downstream_token_id` 并应用数据库过滤。
- [x] 实时日志接口同步支持该筛选；分页、刷新和详情链路保持 ID。
- [x] 增加同名、空名、改名 Token 的精确过滤测试。

**Claude Code（前端）**

- [x] `static/js/dashboard.js` 的 `drillDownToLogs()` 接受 Token ID。
- [x] `static/js/events.js` 点击 Token 排行时传 `data-target-id`，不得退回名称搜索。
- [x] `static/js/logs.js` 将 Token ID 纳入统一筛选状态和请求构造。
- [x] 增加“同名 Token 不串日志”的 Node 行为测试。

**验收**

- [x] 空名称、同名和改名 Token 均只能显示自身历史及实时日志。

---

### P0-B1：Dashboard 时间范围联动

**Codex（后端）**

- [x] 在 `internal/db/log.go` / `internal/handlers/admin.go` 支持 `start`、`end` 的 RFC3339 解析和 `[start, end)` 过滤。
- [x] 分页、游标、刷新、导出（如新增）共用时间范围条件（`LogFilter` 单一入口，游标翻页不越界，已测）。
- [x] 明确最大跨度、缺少一端、逆序范围和非法时区的错误响应（一律 400）。
- [x] 历史范围的实时日志明确暂停，或在服务端过滤到范围内；不得静默混入范围外事件（服务端按同一 `[start, end)` 过滤，历史范围因此天然静默）。
- [x] 增加时间边界、跨时区、分页和实时策略测试。
- [x] **补充（清单原缺口）**：预设窗口解析成具体时刻并回传 `resolved_start` / `resolved_end`，供下钻原样透传。

**Claude Code（前端）**

- [x] 在 `static/js/dashboard.js` 的下钻函数中传递规范化 `start` / `end`。
- [x] 在 `static/js/logs.js` 维护统一范围状态，并让分页、刷新、清空筛选保留范围。
- [x] 在 `static/js/events.js` 处理范围切换、历史范围和实时流开关。
- [x] `static/admin.html` 提供当前日志范围的可见状态。
- [x] 增加从 Dashboard 到日志视图的端到端 DOM/请求参数测试。

**依赖**：依赖 P0-A1 的状态契约；可与 P0-A2 并行，需在联调阶段合并。

### P0-B2：批量测活与健康评级

**Codex（后端）**

- [x] 在 `internal/app/router.go` 注册 `POST /api/admin/upstreams/probe-all`（并注册 `GET` 读取缓存结果）。
- [x] 在 `internal/handlers/upstreams.go` 实现有界并发、单项超时、总体超时和防重复执行。
- [x] 固定结果字段：`upstream_id`、`ok`、`status_code`、`duration_ms`、`error_summary`、`checked_at`。
- [x] 决定最近探针结果使用内存缓存还是 SQLite：**选内存**（`appstate.ProbeRunState`），理由见交接记录。
- [x] 单项失败不影响其他渠道，增加并发上限、超时和部分成功测试。
- [x] **补充（清单原缺口）**：渠道健康统计排除控制台探针 `client_type`，否则批量测活会改写 24h 流量健康。

**Claude Code（前端）**

- [x] 在 `static/admin.html` 增加批量测活入口和结果摘要。
- [x] 在 `static/js/upstreams.js` 处理 loading、防重复点击、部分成功和错误摘要。
- [x] 在现有样式文件中增加“启用状态 / 流量健康 / 最近探针”三类独立徽标。
- [x] 增加批量测活渲染、部分成功和重复点击测试。

**依赖**：后端先冻结结果协议；UI 可先用固定结果 mock 开发。

### P0-B3：高级日志过滤与 TTFB

**Codex（后端）**

- [x] 扩展 `LogFilter`：`downstream_token_id`、`stream`、`min_duration_ms`、`start`、`end`。
- [x] `internal/handlers/admin.go` 统一解析、校验和传递这些参数（抽出 `parseLogFilter`，列表与实时流共用）。
- [x] 确保数据库分页、总数、实时事件（及未来导出）使用同一筛选口径（`Matches` 与 SQL 的等价性已测）。
- [x] 先确定原始时间点：请求接收、上游尝试开始、上游 Header、首个可见 Token、响应完成。
- [x] 如需瀑布图，新增可复算字段；没有真实排队阶段时不得返回虚构的 `queue_ms`（字段命名为 `pre_upstream_ms`，理由见交接记录）。
- [x] 增加筛选组合、NULL duration、流式和非流式测试。

> 筛选与原始时间点采样均已完成；前端四段瀑布接入待做。

**Claude Code（前端）**

- [x] 在 `static/admin.html` 增加 Token、流式/非流式和耗时阈值控件。
- [x] 在 `static/js/logs.js` 将新增筛选接入列表、分页、实时日志和详情。
- [x] 日志详情按原始时间点渲染首字/传输/总耗时；缺失字段显示“不可用”，不估算（已接入 `pre_upstream_ms` / `upstream_headers_ms` 的四段瀑布，见前端 P0-B3 交接记录）。
- [x] 增加筛选状态持久化、清空和组合筛选 Node 测试。

**依赖**：依赖 P0-A4 和 P0-B1 的统一日志筛选状态。

---

### P1-1：SSE 首字前故障转移

**Codex（后端主导）**

- [x] 在 `internal/handlers/proxy.go` / `internal/proxy/client.go` 分离“收到 2xx Header”和“首事件确认成功”。
- [x] 首事件前暂存 Header 与有限前导数据，配置首事件超时、最大缓冲和最大尝试次数（复用渠道 `timeout_seconds` 与全局 `max_retries`，缓冲为代码常量；偏差理由见交接记录）。
- [x] 首事件前的超时、断流、可重试 5xx/429 可切换；首事件后或下游已写入后禁止切换。
- [x] 401/403、3xx 和客户端取消不重试；具体状态矩阵写入测试（`TestRetryableStatusMatrix`）。
- [x] `internal/proxy/ssestream.go` 和日志模型记录失败阶段、渠道、状态和耗时（新增 `failure_stage` / `failure_retryable`）。
- [x] 增加首事件前 EOF、首事件后断流、401/403、429、超时、客户端取消测试。

**Claude Code（前端）**

- [x] 在 `static/js/logs.js` 展示重试次数、渠道尝试链和失败阶段。
- [x] 在 `static/admin.html` / 日志详情中预留重试链展示区域。
- [x] 增加重试链、不可重试错误和流式失败状态的渲染测试。

**依赖**：后端实现和失败注入测试完成后，前端再接入结构化重试链。

### P1-2：Least-Latency 调度

**Codex（后端主导）**

- [x] 在 `internal/proxy/matcher.go` / `internal/proxy/latency.go` 维护有界滚动延迟中位数（未落在 `health.go`，理由见交接记录）。
- [x] 先按 Priority 选择可用层，再在层内按最低延迟选择。
- [x] 明确最小样本、陈旧窗口、无数据回退、异常值和迟滞规则。
- [x] 增加冷启动、样本过期、低样本抖动和 Priority 组合测试。

**Claude Code（前端）**

- [x] 在 `static/js/upstreams.js` 增加策略选择和规则说明。
- [x] 在渠道设置中显示当前策略与回退规则，不自行计算路由结果。
- [x] 增加策略保存、回显和无样本提示测试。

---

### P2-1：Token 模型白名单

**Codex（后端主导）**

- [x] 增加 Token 的 `allowed_models` 存储、迁移和读写 API。
- [x] 鉴权中间件只加载策略；由 Proxy Handler 解析客户端请求模型后校验。
- [x] 以客户端模型匹配白名单，再执行渠道映射；明确别名和通配符规则。
- [x] `/v1/models` 按 Token、Group 和白名单求交集，缓存键包含策略版本。
- [x] 拒绝返回 OpenAI 兼容 403；增加代理、模型列表、缓存失效测试。

**Claude Code（前端）**

- [x] 在 `static/admin.html` / `static/js/tokens.js` 增加模型多选和快捷输入。
- [x] 明确空数组/NULL 的“不限制”展示语义。
- [x] 增加创建、编辑、清空白名单和 403 错误提示测试。

### P2-2：模型定价与成本折算（已整体移除，无待办项）

分工作废。原前后端拆分与勾选状态见 git 历史；当前代码库不含定价与费用估算，另见文末《费用估算功能移除记录》。

### P2-3：周期性配额

**Codex（后端主导）**

- [x] 增加周期类型、时区/锚点、周期键或 generation、周期起止时间。
- [x] reservation、额度检查和晚到 Usage 结算绑定发生时的周期。
- [x] 禁止简单定时清零 `used_tokens`；使用事务或等价原子语义。
- [x] 覆盖月末、夏令时、重启、跨周期在途请求和晚到日志测试。

**Claude Code（前端）**

- [x] 在 `static/js/tokens.js` 增加每日/每周/每月周期配置。
- [x] 显示当前周期、已用/上限、下次重置时间和时区。
- [x] 增加周期切换、无上限、错误回滚和时区展示测试。

---

### P3-1：Prometheus `/metrics`

**Codex（后端主导）**

- [x] 新增 Prometheus 文本格式端点和指标注册。
- [x] 默认关闭或使用独立 Bearer Token/内网策略保护。
- [x] 禁止 Token ID、令牌名称和任意模型字符串标签；控制标签基数。
- [x] 与 `/api/admin/system/metrics` JSON 契约分离。
- [x] 增加格式、鉴权、标签和高基数回归测试。

**Claude Code（前端）**

- [x] 仅增加管理台启用状态、访问策略说明和端点状态展示（如需要）。
- [x] 不在 Dashboard 中复制 Prometheus 原始指标。

### P3-2：配置迁移

**Codex（后端主导）**

- [x] 在现有 `internal/handlers/upstreams.go` 渠道导入导出基础上扩展分组、渠道、Token 策略和系统设置。
- [x] 增加 `schema_version`、应用版本、校验和、稳定迁移键和冲突策略。
- [x] 导入支持 dry-run/预览，校验失败不得部分写入。
- [x] 敏感配置使用 AEAD 和随机盐密码派生；不明文落盘。
- [x] 增加跨实例 Group ID 不同、错误密码、篡改包和版本不兼容测试。

**Claude Code（前端）**

- [x] 提供导出范围、是否包含密钥、导入预览、冲突策略和 dry-run 结果展示。
- [x] 密码输入不回显、不写入 localStorage。
- [x] 增加预览、失败、冲突和成功摘要测试。

### P3-3：SQLite 一致性全量备份与恢复

**Codex（后端主导）**

- [x] 新增一致性快照/在线备份实现，不拼接多表导出作为全量备份。
- [x] 备份包含 schema/application 版本、生成时间、校验和和加密元数据。
- [x] 恢复前生成回滚备份；临时文件校验通过后原子替换。
- [x] 配置迁移 API 与灾备 API 分开授权、审计和路由。
- [x] 增加持续写日志、完整性校验、错误恢复和空白实例恢复测试。

**Claude Code（前端）**

- [x] 提供备份下载、恢复上传、元信息展示和明确的覆盖确认。
- [x] 恢复期间锁定相关操作，并明确是否需要重启。
- [x] 增加错误密码、损坏文件、取消恢复和成功恢复 UI 测试。

---

### 实施顺序与可并行任务

#### 第一批：P0-A

```text
Codex：A1 状态契约、A3 健康口径、A4 Token ID API
Claude Code：A2 加载竞态、A1/A4 前端 mock 与 UI
双方联调：A1 → A4 → P0-B1/P0-B3
```

A1、A2、A3、A4 可以分别开发；但 A1 的状态枚举和 A4 的 Token ID 参数必须先冻结，才能进行日志页联调。

#### 第二批：P0-B

```text
P0-B1 时间范围联动 → P0-B3 高级日志过滤
P0-B2 批量测活可与 P0-B1 并行，但健康卡片必须复用 A3 的数据语义
```

#### 第三批：P1

```text
P1-1 SSE 首字前故障转移 → P1-2 Least-Latency
```

P1-1 是后端高风险任务，必须先完成失败注入测试；前端只在重试链字段冻结后接入详情展示。

#### 第四批：P2/P3

```text
P2-1 白名单 → P2-3 周期配额（P2-2 定价已移除）
P3-1 Prometheus 可独立推进
P3-2 配置迁移 → P3-3 SQLite 灾备恢复
```

### 任务提交与交接格式

每个子任务完成时，执行者在提交说明或任务评论中记录：

```markdown
### 任务
- [ ] 任务编号与标题

### 执行者
- Codex（后端）/ Claude Code（前端）

### 修改文件
- `path/to/file`

### API 或数据契约
- 请求参数：
- 返回字段：
- 空值/错误语义：

### 验证命令
- `go vet ./...`
- `go test ./...`
- 相关 Node 测试

### 交接事项
- 前端需要接入：
- 后端仍未完成：
- 已知平台限制：
```

## 🧪 统一验证门禁

每个任务除专属测试外，至少执行：

1. `go vet ./...`
2. `go test ./...`
3. 与改动相关的 Node 测试；Dashboard/P0-A 基线命令：
   `node --test tests/dashboard-enhancements.test.mjs tests/dashboard-time-range.test.mjs`

涉及并发、配额、流式切换或恢复流程的任务，还应增加定向 race/并发测试和失败注入测试；若 `go test -race ./...` 受平台问题阻塞，必须记录具体包和复现方式，不能把超时记为通过。

---

## 📝 迭代状态记录

| 日期 | 迭代版本 | 变更摘要 | 责任人 |
| :--- | :--- | :--- | :--- |
| 2026-08-18 | v1.2.0 | 完成数据看板说明优化、Top 令牌榜、基础下钻、状态码归因、自动刷新与宽屏适配 | Codex |
| 2026-08-18 | 规划校准 | 结合代码修正完成状态；模型映射和已有调度能力移入 Completed；新增 P0-A 正确性修复；拆分日志、故障转移和灾备范围 | Codex |
| 2026-08-18 | v1.3.0 | 完成 P0-A 全部前后端正确性修复（错误下钻/状态分桶/防竞态/渠道健康/Token稳定ID过滤） | Codex |
| 2026-08-18 | v1.3.1 | 后端完成 P0-B1 时间范围联动与 P0-B3 筛选部分；补上预设窗口 resolved 区间契约；日志筛选解析收敛为单一入口 | Codex |
| 2026-08-18 | v1.3.2 | 后端完成 P0-B2 批量测活与探针结果缓存；修复渠道健康统计长期把控制台探针计为真实流量的既存缺陷 | Codex |
| 2026-08-19 | v1.3.3 | 后端完成 P0-B3 TTFB 原始时间点采样：新增 `request_uid` / `attempt_index` / `pre_upstream_ms` / `upstream_headers_ms`；勾选状态与三份交接记录对齐；确认 race 门禁本机可用 | Claude Code |
| 2026-08-19 | v1.3.4 | 后端补齐 P0-A2 取消验证：覆盖运行中查询取消、Dashboard 聚合、详情读取、连接归还和超时；P0 后端任务全部完成 | Claude Code |
| 2026-08-19 | v1.4.0 | 后端完成 P1-1 SSE 首事件前故障转移与 P1-2 Least-Latency 调度：新增 `failure_stage` / `failure_retryable` / `load_balance_strategy` 与 `GET /api/admin/upstreams/routing`；失败注入与调度测试就位，前端可接入 | Claude Code |
| 2026-08-19 | v1.4.1 | 前端完成 P1-1 SSE 首事件前故障转移与 P1-2 Least-Latency 调度接入：重试链可视化、失败阶段徽标、同层调度摘要条、延迟标签与运行设置持久化全量就位 | Claude Code |
| 2026-08-19 | v1.5.0 | 后端完成 P2 全部三项：Token 模型白名单（含 `/v1/models` 交集与策略指纹缓存键）、版本化模型定价与整数金额快照、周期配额按 period stamp 原子滚动；前端三项均待接入 | Claude Code |
| 2026-08-19 | v1.5.1 | 前端完成 P2 全部三项实施：令牌模型白名单输入/展示、版本化定价规则管理与高精度单价换算、周期性配额周期与时区选择、日志行/详情成本展示与免责声明，164 项测试全绿 | Claude Code |
| 2026-08-20 | v1.6.0 | 校准 P3-1 勾选状态（后端 Prometheus `/metrics` 已于上一次提交完成但清单未勾选）；前端补齐端点状态展示 | Claude Code |
| 2026-08-20 | v1.6.1 | 完成 P3-2 配置迁移包前后端：按名称匹配的可移植归档、AEAD 加密、单事务 dry-run 预览与冲突策略 | Claude Code |
| 2026-08-20 | v1.6.2 | 完成 P3-3 SQLite 一致性备份与恢复前后端：`VACUUM INTO` 快照、结构指纹、暂存—重启原子接管、回滚副本，控制台提供下载/上传/覆盖确认与恢复期锁定；218 项前端测试与全部 Go 测试全绿 | Claude Code |
| 2026-08-20 | v1.7.0 | **移除**费用估算整条功能链（P2-2 作废）：删除结算、价格表 API 与 `model_prices` 表、前端定价界面与日志费用展示，迁移包降去 `pricing` scope 并升 `schema_version` 至 2；日志三列 `cost_*` 作为退役列保留；215 项前端测试与全部 Go 测试（含 race）全绿 | Claude Code |

---

## 🔁 后端交接记录：P0-A（2026-08-18）

### 任务

- [x] P0-A1 错误统计、状态分桶与下钻语义（后端）
- [x] P0-A3 渠道健康统计口径（后端）
- [x] P0-A4 Token 稳定 ID 下钻（后端）
- [x] P0-A2 后端部分：确认 Dashboard 接口的请求 Context 与查询超时

### 执行者

- 后端执行者（Go / SQLite / API）

### 修改文件

- `internal/db/log.go`
- `internal/db/logoverview.go`
- `internal/db/upstreamhealth.go`
- `internal/handlers/admin.go`
- `internal/db/logoverview_test.go`
- 新增：`internal/db/log_status_test.go`、`internal/db/log_token_filter_test.go`、`internal/db/log_match_test.go`、`internal/db/logoverview_status_test.go`、`internal/db/upstreamhealth_test.go`、`internal/handlers/admin_logs_filter_test.go`

### API 或数据契约

- 请求参数（`GET /api/admin/logs` 与 `GET /api/admin/logs/stream` 共用）：
  - `status`：`2xx` / `4xx` / `5xx` / `other` / `none` / `error`
  - `downstream_token_id`：整数，按令牌稳定 ID 精确过滤
  - 既有 `upstream_id`、`search`、`client_type` 语义不变
- 返回字段：
  - `GET /api/admin/logs/overview` 新增 `status_none`；`previous_status` 同步新增 `status_none`
  - `status_other` 现在只表示 1xx/3xx，不再吸收无状态码的行
  - `GET /api/admin/upstreams/health` 的渠道与每个小时桶新增 `timed_count`
- 空值 / 错误语义：
  - `status` 参数存在但不在枚举内返回 400；参数缺省表示不过滤
  - `status` 值两端空白会被 trim 后再校验
  - `downstream_token_id` 无法解析为整数时按“不过滤”处理（沿用既有可选整数参数语义）
  - `none` 仅匹配 `status_code IS NULL`；`error` 匹配 NULL 或任何非 2xx（含 499）
  - 渠道健康中 499 不计入 `errors`，但仍保留在日志与 `status=error` 集合内
  - `timed_count = 0` 表示窗口内没有有效耗时样本，此时 `avg_ms` 为 0；合法 0ms 样本计入 `timed_count`

### 验证命令

- `go vet ./...`（通过）
- `go test ./...`（通过）
- `go test -race ./internal/db ./internal/handlers ./internal/proxy`（通过）
- `node --test tests/dashboard-enhancements.test.mjs`（7 通过）
- `node --test tests/dashboard-time-range.test.mjs`（14 通过）

### 交接事项

- 前端需要接入：
  - 错误时间格与最近失败列表改传 `status=error`，不再固定 `4xx`
  - `other` / `none` / `error` 的图例、Tooltip 与下钻参数按上述枚举对齐
  - Top 令牌下钻改传 `downstream_token_id`，不再依赖名称搜索
  - 实时日志请求可携带同一组筛选参数，服务端已按相同口径过滤
  - 渠道健康卡片使用 `timed_count` 区分“无样本”与“真实 0ms”，并且不把 499 渲染为渠道故障
  - 时间桶下钻可用 `bucket_epoch + bucket_seconds` 构造 `[start, end)`
- 后端仍未完成：
  - P0-A2 的查询取消专项测试
  - 主动探针结果的持久化与展示口径（P0-B2）
  - P0-B1 的 `start` / `end` 参数与 P0-B3 的 `stream` / `min_duration_ms` 筛选
- 已知平台限制：
  - 本轮未新增 SSE 端到端流式测试；实时筛选通过与 SQL 结果比对的等价性测试覆盖（`internal/db/log_match_test.go`）


---

## 🔁 前端交接记录：P0-A（2026-08-18）

### 任务

- [x] P0-A1 错误统计、状态分桶与下钻语义（前端）
- [x] P0-A2 看板时间范围切换防竞态与最后加载生效（前端）
- [x] P0-A3 渠道健康统计口径、timed_count 耗时区分与 UI 文案（前端）
- [x] P0-A4 Token 稳定 ID 下钻与实时流客户端过滤（前端）

### 执行者

- 前端执行者（JavaScript / CSS / DOM / Node Tests）

### 修改文件

- `static/admin.html`
- `static/js/bootstrap.js`
- `static/js/dashboard.js`
- `static/js/events.js`
- `static/js/logs.js`
- `static/js/models.js`
- `static/js/shell.js`
- `static/js/upstreams.js`
- `static/css/dashboard.css`
- `static/css/enhancements.css`
- `tests/dashboard-enhancements.test.mjs`
- `tests/dashboard-time-range.test.mjs`

### 前端改造要点与契约实现

- **状态分桶与错误下钻**：
  - 日志筛选下拉框与下钻参数支持全部 6 种状态口径（`""` / `error` / `2xx` / `4xx` / `5xx` / `other` / `none`）。
  - 看板错误时间细带与“最近失败”头部按钮携带 `status=error` 进行下钻，不再硬编码 `4xx`。
  - 看板状态分布条增加 `other`（1xx/3xx）与 `none`（无响应）展示段及对应颜色与图例环比徽标。
- **范围切换防竞态**：
  - 看板数据加载使用 `dashboardLoadGeneration` 配合 `AbortController` 机制。
  - 快速连续切换时间范围或暂停自动刷新时，前置未决请求被安全中断/丢弃，严格以最后一次选择为准并完成最终渲染。
- **渠道健康统计口径**：
  - 渠道卡片统计展示接入 `timed_count`，正确区分“无有效耗时样本（显示 —）”与真实 0ms。
  - 卡片文案更新为“24h 流量健康”，在线率和均延迟 Tooltip 详细解释统计口径（排除客户端 499 断开、仅统计有耗时采样）。
- **Token 稳定 ID 精确下钻**：
  - Top 令牌排行卡片点击/键盘回车下钻携带 `downstream_token_id`。
  - 日志页支持令牌 Filter Chip 状态徽标与一键清除。
  - 客户端日志列表分页与实时 SSE 流过滤同步支持 `downstream_token_id` 精确匹配。

### 验证命令

- `node --test tests/dashboard-enhancements.test.mjs`（12 全部通过）
- `node --test tests/dashboard-time-range.test.mjs`（15 全部通过）
- `node --test tests/*.test.mjs`（119 全部通过）
- `go vet ./...`（通过）
- `go test ./internal/db ./internal/handlers ./internal/proxy`（通过）

---

## 🔁 后端交接记录：P0-B1 / P0-B3 筛选部分（2026-08-18）

### 任务

- [x] P0-B1 日志时间范围联动（后端）
- [x] P0-B3 高级日志筛选：`stream` / `min_duration_ms`（后端）
- [x] P0-B3 TTFB 瀑布原始时间点采样（已完成）

### 执行者

- 后端执行者（Go / SQLite / API）

### 修改文件

- `internal/db/log.go`：`LogFilter` 增加 `Start` / `End` / `Stream` / `MinDurationMs`；新增 `ResolveWindowRange`
- `internal/db/logoverview.go`：`LogOverviewOut` 增加 `resolved_start` / `resolved_end`
- `internal/handlers/admin.go`：抽出 `parseLogFilter` / `parseLogRange`，列表与实时流共用；overview 回传 resolved 区间
- 新增：`internal/db/log_range_filter_test.go`、`internal/handlers/admin_logs_range_test.go`

### API 或数据契约

**请求参数**（`GET /api/admin/logs` 与 `GET /api/admin/logs/stream` 共用）：

```text
start=<RFC3339>        与 end 必须同时出现，语义 [start, end)
end=<RFC3339>
stream=true|false      也接受 1/0（Go ParseBool 口径）
min_duration_ms=<int>  非负整数，单位毫秒
```

**返回字段**（`GET /api/admin/logs/overview`）新增：

```text
resolved_start  RFC3339 UTC 或 null
resolved_end    RFC3339 UTC 或 null
```

**空值 / 错误语义**：

- `start` / `end` 只给一端返回 400；`start >= end` 返回 400；非 RFC3339（含带 `T` 以外的存储格式 `2026-08-18 00:00:00`）返回 400；跨度超过 366 天返回 400
- 入库前统一转成 UTC 的 `2006-01-02 15:04:05`——`created_at` 就是该格式，直接拿 RFC3339 字面量比较会静默匹配不到任何行，这是本次专门设防的失败模式
- `min_duration_ms` 无法解析或为负返回 400。**注意与既有可选整数语义不同**：`downstream_token_id` 沿用"无法解析即不过滤"，而阈值参数静默失效会让操作者把未过滤的全量列表读成"没有更慢的请求"
- `min_duration_ms` 不匹配 `duration_ms IS NULL` 的行：耗时未知的请求无法证明跨过阈值
- `stream` 非布尔值返回 400
- `resolved_start` / `resolved_end` 在 `range=all` 时为 null，表示下钻不带时间过滤

### 修正：实时流上界（2026-08-19 补）

前端接入后暴露了一个静默失效：看板停在直播预设（如「最近 24 小时」）上下钻时，`resolved_end ≈ now` 会被一并传给 stream 端点，一秒之后这个上界就成了过去，之后到达的每条事件都被判为范围外——实时日志看起来连着，但永远不再出现新行，且没有任何报错。

原先这条约束只写在交接事项里（「直播视图不要给 stream 传 `end`」），靠文档传达不住，接线时就踩了。现在改为后端保证：

```go
// internal/handlers/admin.go
filter = openEndedForLiveStream(filter, time.Now())
```

规则是两段，缺一不可：

- `end` 尚未过去（`>= now`）→ 视为「截至现在、仍在继续」，丢弃上界，新事件照常推送
- `end` 已经过去 → 视为关闭的历史窗口，保留上界，stream 自然静默

只丢上界、永不动 `start`：下界是历史视图不接收更早事件的依据。这也是列表和 stream 唯一一处刻意的口径差异——列表渲染一个固定窗口，stream 渲染接下来到达什么。

测试：`TestOpenEndedForLiveStream`（四种上界形态）、`TestLiveStreamFilterAdmitsEventsAfterConnection`（同一 filter 在列表侧排除、在 stream 侧放行的回归）。

### 关于 resolved 区间（清单原缺口）

清单 P0-B1 要求日志接口收 RFC3339 `start`/`end`，但看板预设窗口（`today`/`1d`/`3d`/`7d`/`30d`）在 `cutoffExpression()` 里是查询时求值的 SQLite 相对表达式，本身不产出任何具体时刻——下钻无从携带。若交给前端自行计算，`today` 的本地午夜口径会和后端漂移。

因此 overview 现在回传它实际统计的那对时刻，前端**原样透传**给日志接口即可，不需要解析或重新格式化。这也是它以 RFC3339 而非内部存储格式输出的原因：直接就是日志接口能收的形状。测试里断言了这对值能通过 `parseLogRange` 往返。

### 验证命令

- `go vet ./...`（通过）
- `go test ./...`（全部通过）
- `go test ./internal/db -run 'TestListLogsFiltersByCreatedAtRange|TestListLogsRangePagesWithoutLeaking|TestListLogsFiltersByStreamAndDuration|TestRangeFiltersMatchesAgreesWithSQL|TestResolveWindowRange'`（通过）
- `go test ./internal/handlers -run 'TestAdminListLogsAppliesTimeRange|TestAdminListLogsAppliesStreamAndDurationFilters|TestAdminListLogsRejectsInvalidRangeAndThreshold|TestParseLogFilterContract|TestLogOverviewReportsResolvedRange'`（通过）
- `node --test tests/*.test.mjs`（119 全部通过，未因本次改动回归）

### 交接事项

**前端需要接入**：

- 下钻时把 overview 的 `resolved_start` / `resolved_end` 原样作为 `start` / `end` 传给日志接口；两者为 null（`range=all`）时不要带时间参数
- 分页、刷新、清空其他筛选时保留范围；`start`/`end` 必须成对出现，只带一端会 400
- 新增 `stream`、`min_duration_ms` 控件；后者非法值会 400，需要在 UI 侧先做基本校验避免可预期的报错
- **实时流的范围语义（已改为后端保证，前端无需特殊处理）**：stream 端点会自动丢弃"尚未过去"的上界。传 `end ≈ now`（直播预设的 resolved 区间）时它按开区间处理，新事件照常推送；传已经关闭的历史 `end` 时保留上界，于是自然静默（等于暂停）。前端可以对两个端点传同一组范围参数，不需要区分直播/历史
  - 该规则由 `openEndedForLiveStream` 实现，见后端交接记录《修正：实时流上界》
- 阈值筛选不会返回 `duration_ms` 为空的行，UI 文案应体现"仅统计有耗时采样的请求"

**后端仍未完成**：

- P0-B3 的 TTFB 原始时间点采样与瀑布字段（请求接收 / 上游尝试开始 / 上游 Header / 首个可见 Token / 响应完成）
- P0-B2 批量测活 `probe-all` 与探针结果持久化
- P0-A2 的查询取消专项测试（沿用上一轮未完成项）

**已知平台限制**：

- `go test -race` 在本机无法执行：Windows 环境缺 gcc，`CGO_ENABLED=1 go test -race ./internal/db ./internal/handlers` 报 `cgo: C compiler "gcc" not found`。本次改动为纯查询构造与参数解析，未引入新的共享可变状态；race 门禁需在装有 gcc/clang 的环境补跑
- 本轮未新增 SSE 端到端流式测试；实时筛选仍通过 `Matches` 与 SQL 的等价性测试覆盖（`internal/db/log_range_filter_test.go`）
- `AdminStreamLogs` 的筛选无法在 handler 层做端到端测试：它先校验管理员鉴权，而 `adminAuthKey` 是 middleware 包私有的。改为直接测共享的 `parseLogFilter`，这也是两个端点真正共用的那部分

---

## 🔁 后端交接记录：P0-B2 批量测活（2026-08-18）

### 任务

- [x] P0-B2 批量测活与探针结果缓存（后端）
- [x] 附带修复：渠道健康统计排除控制台探针流量

### 执行者

- 后端执行者（Go / SQLite / API）

### 修改文件

- `internal/appstate/state.go`：新增 `ProbeResult`、`ProbeRunState`（`TryStart` / `Finish` / `Abandon` / `Snapshot` / `Forget`）
- `internal/handlers/upstreams.go`：新增 `AdminProbeAllUpstreams`、`AdminLastProbeResults`、`probeUpstreamsConcurrently`、`probeOneUpstream`、`probeClientTypes`；`probeOutcome` 增加 `durationMs`（失败路径也返回）
- `internal/db/upstream.go`：新增 `ListUpstreamRows`
- `internal/db/upstreamhealth.go`：`UpstreamHealthHistory` 增加 `excludeClientTypes` 参数
- `internal/handlers/admin.go`：健康端点传入 `probeClientTypes()`
- `internal/app/router.go`、`internal/app/server.go`：注册路由、装配 `ProbeRuns`
- `internal/handlers/upstream_detail_test.go`：共享测试 state 补 `ProbeRuns`
- 新增：`internal/handlers/upstream_probe_all_test.go`；`internal/db/upstreamhealth_test.go` 增加探针排除测试

### API 或数据契约

**`POST /api/admin/upstreams/probe-all`**

```text
查询参数：include_disabled=true   默认只测启用渠道
```

响应：

```json
{
  "total": 3, "probed": 3, "skipped": 0,
  "succeeded": 1, "failed": 2, "partial": false,
  "results": [
    {"upstream_id": 1, "ok": true, "status_code": 200,
     "duration_ms": 12, "error_summary": null,
     "checked_at": "2026-08-18 07:30:00"}
  ]
}
```

**`GET /api/admin/upstreams/probe-all`** 读最近一次结果，不发起探针：

```json
{"running": false, "checked_at": "2026-08-18 07:30:00", "results": [...]}
```

**空值 / 错误语义**：

- 探针内容与单渠道测试一致（`GET /v1/models`），所以批量结果和手动测单个渠道不会给出不同答案
- `ok` 判据是 `status_code < 400`
- 传输层失败（连不上、超时）`status_code` 为 null，但 `duration_ms` 和 `error_summary` 仍然有值——失败得多快是这种渠道唯一能给的信息，否则「连不上」和「没测」在响应里长得一样
- 渠道自身配置坏掉（`extra_headers` 解析失败等）也会作为一条失败结果出现，不会从报告里消失
- `error_summary` 截断到 300 字符，完整内容在对应的日志行里
- 已有测活在跑时第二个请求返回 **409**，不排队：第一个跑完就是第二个想要的答案
- 并发上限 6，整体预算 90s，单项沿用渠道自己的 `timeout_seconds`
- 超预算时**发布已完成的部分**并置 `partial: true`、`skipped > 0`；没轮到的渠道不写入缓存，避免把「没测」缓存成失败

### 两个设计决定

**1. 探针结果存内存，不落 SQLite。** 清单说「生产依赖场景优先持久化」，但这份数据描述的是某一瞬间的可达性，重启走完就已经过期，落库反而会让人把旧行读成当前状态。代价是重启后徽标空白到下次测活为止——已写进契约。真要跨重启保留，应该是独立的探针历史表（带时间序列），而不是缓存最后一行。

**2. 修了健康统计的探针污染。** 这是本次动手前发现的既存 bug，不是批量测活引入的：`UpstreamHealthHistory` 从来没过滤 `client_type`，所以单渠道测试、模型测试、模型列表、余额查询**早就在**污染「24h 流量健康」了。批量测活只是会把它放大到整个看板。

修法是把排除集合作为参数从 handlers 传进去——那一层本来就拥有这些 `client_type` 常量。没有把常量搬到 `models`，因为 `tests/console-probe-logging.test.mjs` 用正则从 `internal/handlers/upstreams.go` 里抓这些字面量，搬走会让前端拥有的测试失效（协作规则 4）。

> ⚠️ 新增探针类型时必须同步加进 `probeClientTypes()`，否则健康统计会静默把它当成真实流量。

### 验证命令

- `go vet ./...`（通过）
- `go test ./...`（全部通过）
- `go test ./internal/handlers -run TestProbeAll -v`（6 个子测试通过：各渠道独立结论、默认跳过禁用渠道、并发拒绝、并发上限、结果缓存与 Forget、坏配置渠道）
- `go test ./internal/db -run TestUpstreamHealth -v`（3 个通过，含新增的探针排除）
- `node --test tests/*.test.mjs`（119 全部通过，未回归）

### 交接事项

**前端需要接入**：

- 渠道列表加载时调 `GET /api/admin/upstreams/probe-all` 回填徽标，不要每次进页面就 `POST`
- `POST` 期间禁用按钮；收到 409 说明别处正在跑，应改为轮询 `GET` 而不是提示失败
- `partial: true` 时要区分「测了且失败」和 `skipped`（没轮到）——后者不该显示成红色故障
- 三类徽标口径互不相同，别合并：启用状态（配置）、24h 流量健康（真实代理流量，现已排除探针）、最近探针（一次人工触发的合成请求）
- `status_code` 为 null 且有 `error_summary` 的是传输层失败，文案应区别于 4xx/5xx
- 重启后 `results` 为空、`checked_at` 为 null，属正常状态，显示「尚未测活」而非错误

**后端仍未完成**：

- P0-B3 的 TTFB 原始时间点采样与瀑布字段
- P0-A2 的查询取消专项测试
- 探针结果的跨重启持久化（若产品确认需要，应设计为独立历史表）

**已知平台限制**：

- `go test -race` 仍无法在本机执行（缺 gcc）。本次**确实引入了新的并发代码**（`ProbeRunState` 的锁、`probeUpstreamsConcurrently` 的 worker pool 与共享 results 切片），比上一轮更需要 race 门禁。切片写入按 index 分区、无重叠，锁覆盖所有共享字段，但这需要在装有 gcc/clang 的环境跑 `go test -race ./internal/handlers ./internal/appstate` 确认
- 整体预算超时后的 `partial` 路径没有专项测试：稳定复现需要能挂住 90s 的假上游，代价高于收益。`skipped` 的记账逻辑通过 `probeUpstreamsConcurrently` 过滤空 `CheckedAt` 的分支覆盖


---

## 🔁 前端交接记录：P0-B1 / P0-B2 / P0-B3 前端全量实施（2026-08-18）

### 任务
- [x] **P0-B1 看板下钻时间范围联动（前端）**：下钻自动携带 `resolved_start` / `resolved_end` 及标签，日志视图提供范围徽标、可独立清除，分页/刷新/清空筛选保持范围状态一致。
- [x] **P0-B2 渠道一键批量测活与健康徽标（前端）**：渠道列表顶部提供「一键测活」按钮与实时状态条；页面加载只读 `GET /probe-all`；触发 `POST` 防重入并处理 409 降级轮询；卡片与表格解耦展示「启用状态 / 24h 流量健康 / 最近主动探针」三类徽标，覆盖 OK、失败、传输层异常、已跳过、未测活等状态。
- [x] **P0-B3 日志高级过滤与 TTFB 耗时拆解（前端）**：日志栏新增流式/非流式选择框及耗时阈值 (`min_duration_ms`) 输入；打通列表、分页、实时 SSE 与清空；日志详情对话框增加首字 (TTFB) 与传输耗时双段进度条，首字缺失标记为不可用，非流式不虚构排队耗时。

### 修改文件
- `static/admin.html`：新增一键测活按钮、测活状态栏、日志时间范围徽标、流式选择框、耗时阈值输入框。
- `static/css/enhancements.css`：新增 `probe-badge` 系列样式、测活 summary 状态条动画、TTFB 耗时进度条样式。
- `static/js/bootstrap.js`：集中管理 `logRangeStart`, `logRangeEnd`, `logRangeLabel` 状态与徽标更新方法。
- `static/js/dashboard.js`：下钻函数 `drillDownToLogs` 继承 `resolved_start` / `resolved_end`，无缝跳转至日志。
- `static/js/upstreams.js`：实现 `fetchLatestProbeResults`、`runBatchProbe`、`renderProbeSummary`、`renderProbeBadgeMarkup`，解耦三种徽标。
- `static/js/logs.js`：`loadLogs` 与 `openLogStream` 接入 `start` / `end` / `stream` / `min_duration_ms`；`formatLogDetailMeta` 渲染 TTFB/传输耗时条。
- `static/js/models.js` & `static/js/events.js`：绑定一键测活、流式选择、耗时阈值及时间范围清除事件。
- `static/js/shell.js`：`clearLogFilters` 统一重置所有新筛选条件。
- `tests/batch-probe.test.mjs`：新增 7 组端到端单元与 DOM 状态测试（全部通过）。
- `tests/logs-filters.test.mjs`：新增 6 组时间范围联动、TTFB 拆解与高级过滤测试（全部通过）。

### 验证命令
- `node --test tests/*.test.mjs`（137 项测试全部通过）
- `go test ./... -count=1`（Go 后端测试全部通过）

---

## 🔁 后端交接记录：P0-B3 TTFB 原始时间点采样（2026-08-19）

### 任务

- [x] P0-B3 原始时间点采样与可复算瀑布字段（后端）
- [x] 附带：勾选状态与既有三份交接记录对齐
- [x] 附带：确认 `go test -race` 在本机可用，撤销此前记录的平台限制

### 执行者

- 后端执行者（Go / SQLite / API）

### 修改文件

- `internal/db/sql.go`：`request_logs` 新增 4 列及其语义注释
- `internal/db/schema.go`：4 列的 `ensureColumn` 迁移
- `internal/models/requestlog.go`：`RequestLogOut` 新增 4 字段
- `internal/proxy/logging.go`：`LogEntry` 新增 4 字段；INSERT 与实时流事件同步
- `internal/proxy/client.go`：`RequestContext` 增加 `RequestUID` / `AttemptIndex` / `ReceivedAt`；`baseLogEntry` 接收 attempt 原点；新增 `preUpstreamMs`；采样 Header 到达时刻
- `internal/handlers/proxy.go`：`abortLogGuard` 持有 `requestUID`；新增 `newRequestUID`；重试循环透传 uid / attempt / 接收时刻
- 新增：`internal/proxy/timing_sample_test.go`、`internal/handlers/proxy_timing_chain_test.go`、`internal/db/log_timing_test.go`

### 五个原始时间点与四段区间

```text
received_at ──pre_upstream_ms──▶ 尝试开始 ──upstream_headers_ms──▶ 上游 Header
   (新)                          (原点)          (新)
            ──first_token_ms──▶ 首个可见 Token ──▶ 响应完成 (duration_ms)
                  (已有)                            (已有)
```

原点是「本次尝试开始」，未改动。`first_token_ms` / `duration_ms` 语义与历史数据均不受影响，`upstream_headers_ms` 与它们共享同一原点，因此三者必然满足 `headers ≤ first_token ≤ duration`——测试直接断言这个嵌套关系，因为原点写错时它是第一个断掉的性质。

### API 或数据契约

**返回字段**（`GET /api/admin/logs`、`/logs/:id`、`/logs/stream` 三处一致）：

```text
request_uid          string 或 null   同一请求各次尝试的关联键
attempt_index        int 或 null      首次尝试为 0
pre_upstream_ms      int 或 null      网关接收 → 本次尝试发起上游调用
upstream_headers_ms  int 或 null      尝试开始 → 上游响应头到达
```

**空值语义**（每条都是刻意区分，不是缺省）：

- `attempt_index = null` ≠ `0`：null 表示这一行从未到达任何上游尝试（无路由、限流拒绝、读 body 期间客户端断开）；`0` 表示确实发起过第一次尝试
- `pre_upstream_ms = null`：调用方未采样 `ReceivedAt`（控制台探针即如此）。零值 `time.Time` 被显式挡掉——否则会算出几十年的网关耗时并当作真实数据渲染
- `upstream_headers_ms = null`：Header 从未到达（传输层失败、连不上、超时）。**body 读取失败时该字段有值**：Header 已经到了，是 body 断的，这正好区分「答得快然后中途卡死」和「压根没答」
- `request_uid = null`：仅出现在迁移前的历史行，或系统随机数读取失败时（此时不拒绝请求，关联键不值得让代理失败）
- 迁移前的历史行四个字段全为 null，代表「从未采样」，前端应显示不可用而非 0

### 为什么不叫 `queue_ms`

清单要求「没有真实排队阶段时不得返回虚构的 `queue_ms`」。这里确实没有队列，但网关在发起上游调用前的耗时是真实存在的：读下游 body、选路、限流准入、`PrepareRequest`。所以字段按测量区间命名为 `pre_upstream_ms`。

一个必须和 `attempt_index` 一起读的性质：attempt 0 上它是纯网关开销；attempt 1 及以后它**含前序尝试全程与重试退避**，所以同一请求内它单调不减。测试 `TestAttemptChainIsQueryableByRequestUID` 和 `TestRetriedAttemptsShareOneRequestUID` 都断言了这一点。若命名为 `queue_ms`，控制台几乎必然把重试行的 1130ms 显示成「排队 1.1 秒」。

### 顺带确认的既有行为（非本次改动）

写重试链测试时发现：`internal/handlers/proxy.go` 的重试**不排除刚失败的渠道**——只有被限流的渠道进 `rateLimited` 集合。所以单渠道失败时，重试会再次选中同一渠道（撞上 `SameUpstreamRetryIntervalMs` 退避）。这属于 P1-1 的「校准可重试错误」范围，本次未改，测试按实际行为编写。

### 验证命令

- `go vet ./...`（通过）
- `go test ./... -count=1`（全部通过）
- `go test -race ./internal/proxy ./internal/handlers ./internal/db -count=1`（通过）
- `go test ./internal/proxy -run 'TestBufferedResponseRecordsHeaderArrival|TestStreamRecordsHeadersBeforeFirstToken|TestTransportFailureLeavesHeaderSampleNull|TestUnsampledOriginLeavesPreUpstreamNull|TestPreUpstreamMsClampsAndSkips' -v`（5 通过）
- `go test ./internal/handlers -run 'TestRetriedAttemptsShareOneRequestUID|TestRejectionBeforeAnyAttemptHasNoAttemptIndex' -v`（2 通过）
- `go test ./internal/db -run 'TestTimingColumnsMigrateOntoAnOlderDatabase|TestTimingSamplesRoundTripThroughBothReadPaths|TestAttemptChainIsQueryableByRequestUID' -v`（3 通过）
- `node --test tests/*.test.mjs`（137 全部通过，未回归）

### 交接事项

**前端需要接入**：

- 日志详情的耗时条从两段改为四段：`pre_upstream_ms`（网关）、`upstream_headers_ms`（连接+上行）、`first_token_ms - upstream_headers_ms`（上游生成）、`duration_ms - first_token_ms`（传输）。目前 `static/js/logs.js:1530` 是 `duration - first_token` 两段
- 任一字段为 null 时该段显示「不可用」，不要用 0 或推导值补位
- `pre_upstream_ms` 的文案必须随 `attempt_index` 变化：`0` 显示「网关准备」；`>0` 需说明含前序尝试与退避，不能写成「排队」
- 非流式行 `first_token_ms` 恒为 null（后端只对流式采样），四段退化为两段，属正常
- `request_uid` 可用于把同一请求的多次尝试折叠展示，这也是 P1-1 重试链 UI 的挂载点

**后端仍未完成**：

- P1-1：可重试错误矩阵、SSE 首事件前切换。重试链的关联键已就绪，P1-1 只需往这对键上补失败阶段字段，不需要再次迁移

> P0-A2 的查询取消测试已于同日补齐，见下一节交接记录。

**已知平台限制**：

- 此前记录的「本机缺 gcc、race 跑不了」**已不成立**：gcc 13.2.0（MinGW-W64）在，`CGO_ENABLED=1`，`go test -race ./...` 全包通过。相应的历史限制说明应视为过期
- 未新增 SSE 端到端超时/断流的注入测试；`TestStreamRecordsHeadersBeforeFirstToken` 用真实 httptest 流覆盖了正常路径的采样顺序


---

## 🔁 前端交接记录：P0-B3 原始耗时采样与四段瀑布可视化（2026-08-19）

### 任务

- [x] **P0-B3 原始耗时采样四段瀑布与重试标识全量接入（前端）**：
  - 日志详情弹窗接入后端 4 列原始采样字段：`pre_upstream_ms`、`upstream_headers_ms`、`attempt_index`、`request_uid`。
  - 流式请求渲染 4 段式耗时瀑布进度条：
    1. 网关准备（`pre_upstream_ms`，若 `attempt_index > 0` 显示「网关准备（含前序重试与退避）」）；
    2. 连接与响应头（`upstream_headers_ms`）；
    3. 上游模型生成（`first_token_ms - upstream_headers_ms`）；
    4. 响应传输（`duration_ms - first_token_ms`）。
  - 非流式请求优雅降级为 2/3 段（网关准备 + 连接/Header + 响应传输），不虚构首字生成耗时。
  - 缺失字段明确标记为「不可用」，历史未迁移数据无缝回退至既有两段/总耗时逻辑。
  - 重试请求（`attempt_index > 0`）在路由卡片展示 `重试 #N` 徽标及关联 `request_uid`。

### 修改文件

- `static/js/logs.js`：
  - 新增 `formatGatewayPrepTime(ms, attemptIndex)` 与 `formatHeadersArrivalTime(ms)`；
  - 升级 `formatLogDetailMeta(detail)`，支持 4 段式瀑布进度条与文案计算、重试文案与徽标；
  - 完善空值与历史数据兼容。
- `static/css/enhancements.css`：
  - 新增 `.timing-seg--gateway`、`.timing-seg--headers`、`.timing-seg--generation`、`.timing-seg--ttfb`、`.timing-seg--transfer` 进度条分段主题配色；
  - 新增 `.log-detail-route-attempt` 重试徽标样式。
- `tests/logs-filters.test.mjs`：
  - 新增 4 段式流式耗时进度条数学比例与文案断言测试；
  - 新增重试标识与关联 `request_uid` 渲染测试；
  - 新增非流式采样耗时进度条与文案断言测试；
  - 新增空值不可用与历史数据回退测试；
  - 新增 CSS 样式与辅助函数单测。
- `清单.md`：
  - 更新 P0-B3 前端瀑布完成状态，全量闭环 P0 阶段所有前端与后端任务。

### 验证命令与结果

- `node --test tests/*.test.mjs`（142 测试全部通过）
- `go vet ./...`（通过）
- `go test -short ./... -count=1`（全部通过）
- `go test ./internal/db ./internal/handlers ./internal/proxy -count=1`（全部通过）

---

## 🔁 后端交接记录：P0-A2 查询取消验证（2026-08-19）

### 任务

- [x] P0-A2 后端：handler / 查询取消测试（此前连续三轮记为未完成）
- [x] 附带修复：共享测试 state 缺失 `LogStats` 装配

### 执行者

- 后端执行者（Go / SQLite / API）

### 修改文件

- `internal/handlers/upstream_detail_test.go`：`upstreamTestState` 补 `LogStats`
- 新增：`internal/db/log_cancellation_test.go`、`internal/handlers/admin_logs_cancel_test.go`

### 先实测驱动行为，再写断言

此前三轮把这项挂着的理由是「未验证 SQLite 驱动在取消场景下的稳定可观测行为」。本轮先用一次性探针测了 `modernc.org/sqlite` 的实际表现（4 万行、无索引前缀通配搜索）：

```text
未取消基线              180ms
运行中取消后返回         11ms   err = context canceled
后续查询                502µs  连接已归还，InUse = 0
```

行为是稳定的，所以断言可以写实。断言选的是三条性质，不是错误文案：

1. **通过 `errors.Is` 断言取消原因**，不比对字符串。驱动措辞不是契约，但原因原样上传是 handler 区分「请求被放弃」和「真实数据库故障」的依据（`apperr.AppError` 有 `Unwrap`，链路完整）。
2. **耗时必须显著低于同一次运行内实测的基线**（阈值取基线一半）。基线在测试内现算而非硬编码，所以换机器不会失效；11ms vs 180ms 的实际差距远大于这个松阈值，不会被调度噪声打成偶发失败。
3. **连接必须归还**。测试池 `SetMaxOpenConns(1)`，所以泄漏不是缓慢劣化——下一个查询永远拿不到连接，会挂到 15s 超时。这把「生产高负载下才暴露」的问题变成了确定性失败。

### 覆盖范围

`internal/db/log_cancellation_test.go`（4 个）：

- `TestACancelledListingGivesUpMidFlight`：运行中取消。既有 `TestALogListingGivesUpRatherThanHoldingItsConnection` 测的是「预先取消时拒绝开始」，这条测的是「已在跑时放弃」，两者不重复
- `TestCancelledAggregationsReleaseTheirConnection`：overview / top / token-usage / upstream-health 四条聚合路径各自独立，任一条泄漏都会拖住代理路径（共用同一连接池），所以逐个测
- `TestLogQueryTimeoutIsBoundedByItsOwnDeadline`：用 1ns 父截止代替等满 10s 的 `LogQueryTimeout`，断言 `DeadlineExceeded` 上传
- `TestACancelledDetailLookupDoesNotStrandItsConnection`：额外断言取消后该行仍可读——取消不能被误当成「行不存在」，否则下钻会把日志报成已删除

`internal/handlers/admin_logs_cancel_test.go`（3 个）：

- `TestAbandonedLogListingDoesNotOutliveItsRequest`：范围切换竞态的 handler 一半，客户端放弃后请求必须停下
- `TestCancelledDashboardEndpointsStayAvailableAfterwards`：四个端点取消后仍可服务
- `TestACancelledListingIsNotReportedAsAnEmptyPage`：断言取消**不会**返回 200 空页。这是最坏的失效形态——前端 generation 机制会丢弃过期响应，但若当前 generation 收到 200 + 零行，会渲染成「无匹配日志」，操作者会当成关于自己流量的事实

### 两个顺带发现

**1. 共享测试 state 缺 `LogStats`。** `AdminTokenUsageStats`（`internal/handlers/admin.go:768`）在读任何参数前先取 `state.LogStats.Snapshot()`，而 `upstreamTestState` 从未装配它——空指针 panic。生产侧 `internal/app/server.go:117` 是装配的，所以这只是测试夹具与生产装配不一致，非生产缺陷。已按生产形状补齐。

**2. token-usage 的预设窗口不查库。** 该端点只在 **custom** 范围才调 `QueryTokenUsage`，`today`/`1d`/`7d`/`30d`/`all` 全部由内存快照直接返回。所以拿 `range=all` 测取消会「通过」但压根没碰到 SQLite。测试因此显式用 custom 范围，注释里写明了原因，避免以后有人改回预设值而看不出覆盖已经失效。

### 验证命令

- `go vet ./...`（通过）
- `go test ./... -count=1`（全部通过）
- `go test -race ./internal/db`（通过）、`go test -race ./internal/handlers ./internal/proxy`（通过）
- `go test ./internal/db -run 'TestACancelledListingGivesUpMidFlight|TestCancelledAggregationsReleaseTheirConnection|TestLogQueryTimeoutIsBoundedByItsOwnDeadline|TestACancelledDetailLookupDoesNotStrandItsConnection' -v`（4 通过，含 4 个子测试）
- `go test ./internal/handlers -run 'TestAbandonedLogListingDoesNotOutliveItsRequest|TestCancelledDashboardEndpointsStayAvailableAfterwards|TestACancelledListingIsNotReportedAsAnEmptyPage' -v`（3 通过，含 4 个子测试）
- `node --test tests/*.test.mjs`（142 全部通过，未回归）

### 交接事项

**前端需要接入**：无。本轮只补测试与测试夹具，未改任何契约或业务行为。

**后端仍未完成**：

- P1-1 全部（可重试错误矩阵、SSE 首事件前切换、失败阶段字段）
- P1-2 Least-Latency 调度

> P0 后端任务至此全部完成。

**已知平台限制**：

- 取消后的 ERROR 日志文案偏重：客户端放弃请求会走 `apperr.WriteError` 的 `KindDatabase` 分支，在日志里留下 `ERROR database error error="context canceled"`。对客户端无影响（连接已断，500 写不出去），但看板被频繁切换范围时会刷出一串看着像真实故障的 ERROR 行。未改动，因为这属于清单已勾选的「可观测日志」子项，改它会超出本项范围——若要收敛，建议在 `WriteError` 里对 `errors.Is(err, context.Canceled)` 降级为 DEBUG 并跳过响应写入
- `go test -race` 在本机会偶发 `bufio: buffer full` 构建错误，成因是构建缓存被并发访问/污染，与被测代码无关；单包重跑即通过（`db` 207s、`handlers` 67s 均为清缓存后全量重建）。另偶发 Windows 文件锁导致 `.test.exe` 无法写入，同样重跑即过

---

## 🔁 后端交接记录：P1-1 SSE 首事件前故障转移 / P1-2 Least-Latency 调度（2026-08-19）

### 任务

- [x] P1-1 后端：可重试矩阵、首事件闸门、失败阶段字段、六类失败注入测试
- [x] P1-2 后端：有界滚动延迟、层内最低延迟、冷启动/过期/抖动/Priority 组合测试
- [x] 附带：`GET /api/admin/upstreams/routing`（前端做 P1-2 UI 需要它）

### 执行者

- 后端执行者（Go / SQLite / API）

### 修改文件

新增：

- `internal/proxy/failover.go`：失败阶段枚举、可重试判定、首事件闸门 `awaitFirstSSEEvent`
- `internal/proxy/latency.go`：`LatencyTracker`（有界环 + 中位数 + 陈旧窗口）
- `internal/proxy/failover_test.go`、`internal/proxy/latency_test.go`
- `internal/handlers/proxy_failover_test.go`、`internal/handlers/upstream_routing_test.go`

改动：`internal/proxy/{client,sse,ssestream,bufferedstream,logging,matcher}.go`、`internal/handlers/{proxy,admin,upstreams}.go`、`internal/models/{requestlog,settings,upstream}.go`、`internal/db/{sql,schema,settings,log}.go`、`internal/appstate/state.go`、`internal/app/{server,router}.go`

### 「首事件」定义为一个完整 SSE 事件，不是首个可见 token

推理模型可以先思考几分钟再吐字。若把「首个可见 token」当作成功信号，闸门会在渠道正常工作时把它判死并切换——切换后新渠道同样要思考，于是重试预算被完全浪费在一个没有故障的请求上。

判定单位因此是 SSE 帧：一个或多个非空、非注释行后跟一个空行。`:` 开头的心跳被显式排除（`internal/proxy/sse.go` 的 `observeLine`），否则一个只会喘气、永不作答的渠道会被当作已应答。

一个副作用必须保留：上游发完完整答案却没有结尾空行就关闭时，`finish()` 会补齐这个未闭合事件。缺了它，闸门会抛弃一个确实已经作答的流。

### 三处与清单文字的偏差

清单写的是「设置首事件等待超时、最大前导缓冲和总尝试上限」。实际实现复用了既有配置，没有新增三个设置项：

| 清单要求 | 实际来源 | 理由 |
| :--- | :--- | :--- |
| 首事件等待超时 | 渠道既有 `timeout_seconds` | 它本就是「上游静默多久算超时」。闸门内**故意不 extend** 这个时钟，所以等待被渠道自己的超时封顶 |
| 最大前导缓冲 | 代码常量 `maxSSELeadBytes = 64KB` | 它约束的是单流内存占用，不是运营策略。做成设置项会让操作者面对一个调错就会 OOM 或误切的旋钮 |
| 总尝试上限 | 既有 `max_retries` | 再加一个「首事件尝试上限」会与它冲突，两个上限谁生效需要第三条规则解释 |

若后续要把首事件超时做成独立可调项，需要同时加表单项、校验和回显；本轮没做，因为清单没有对应的前端项，加了就是一个操作者看不见的设置。

### 达到缓冲上限时提交而非切换

`awaitFirstSSEEvent` 在攒到 64KB 仍未见完整事件时**返回成功**。理由：渠道在持续发送，只是格式不合预期；把正在发送数据的连接判死会让一个协议略有差异的渠道永远无法服务，而这些字节已经攒在手里，直接交付比丢弃更符合调用方意图。测试 `TestGateCommitsOnceTheLeadBufferFills` 固定了这个行为，并断言攒下的字节全部下发。

### 闸门只在还有下一次尝试时才启用

`RequestContext.FailoverEligible` 由 handler 按 `attempt < maxRetries` 传入。最后一次尝试不经闸门，行为与闸门存在之前完全一致：Header 一到就交付。为一个没人能执行的决定付延迟没有意义。

这条有一个必须知道的后果：**单渠道部署下，空 SSE 流的最后一次尝试仍会返回 200**。第一次尝试被判为 `first_event` 可重试失败，第二次因无处可切而保留旧行为。`TestSingleChannelLastAttemptPreservesHeaderSuccess` 固定了这个语义——它不是缺陷，但前端展示重试链时会看到「首次失败、末次 200」的组合，不要当成数据错乱。

### 状态矩阵与新增日志字段

`failure_stage` 取值：`request_build` / `connect` / `upstream_status` / `first_event` / `response_body` / `stream` / `client_cancelled` / `no_route` / `rate_limited` / `gateway`。成功的行两个字段都是 NULL——没有引入 `none` 值，否则每个控制台筛选都要手工排除它，第一个忘记的地方就会把成功报成失败。

`failure_retryable` 由阶段决定，唯一例外是 `upstream_status` 由状态码决定：5xx / 408 / 425 / 429 可重试；401 / 403 / 407、400 / 404 / 409 / 413 / 422、3xx 不可重试。

**`failure_retryable` 不表示「发生过重试」。** 预算耗尽或无其他渠道可路由时，最后一次尝试同样标 `true`。要判断是否真的重试过，读 `request_uid` + `attempt_index`。

### 健康分归属未改动

客户端取消（`client_cancelled`）不计入渠道健康分；其余非 2xx 一律计入，包括 3xx / 400 / 404。考虑过把这几类改成健康中性，最终没改：一个 base URL 配错、把所有请求 404 掉的渠道，目前会被扣分扣出轮转；改成中性后它会保持满权重继续吃流量。这是一个独立的判断，不该夹在重试矩阵里顺手改掉。

### P1-2 用中位数而非均值，落点在新文件而非 health.go

清单写的是「`internal/proxy/matcher.go` / `internal/proxy/health.go`」。延迟采样实际落在新的 `internal/proxy/latency.go`：`health.go` 管的是失败扣分与恢复，延迟是另一个维度，混在一起会让两套衰减规则共享同一份状态。

**中位数不是均值**：一次 30 秒的冷启动会把 8 个样本的均值拉到 3.7 秒以上，并在整个陈旧窗口内让该渠道排在所有竞争者之后。中位数直接忽略它——这就是清单要求的「异常值处理」，不需要额外的裁剪规则。

规则常量（都在 `internal/proxy/latency.go`，控制台通过路由端点读取，不要在前端复制一份）：

| 常量 | 值 | 作用 |
| :--- | :--- | :--- |
| `LatencyMinSamples` | 5 | 低于此视为「未测量」，仍参与竞争 |
| `LatencyStaleWindow` | 5 分钟 | 超期样本丢弃，不做降权 |
| `LatencySampleCapacity` | 32 | 单渠道环大小 |
| `LatencyToleranceRatio` | 0.2 | 容忍带比例（迟滞） |
| `LatencyToleranceFloorMs` | 50 | 容忍带下限 |

三条回退语义，前端的「无样本」提示应当按此措辞：

1. **无任何可用样本** → 该层退化为纯加权随机，与策略未启用时完全一致。
2. **样本不足 5 个** → 视为未测量，**保留在竞争中**。排除它是死锁：不被选中就永远测不到样本。
3. **样本过期** → 整环删除，报告为未测量而非「慢」。

容忍带是迟滞手段：只按严格最小值排序，会让每个请求都涌向此刻快 2ms 的那个渠道，负载压上去之后它变慢，再全部涌回——这就是清单要避免的抖动。50ms 下限防止两个都很快的渠道（20ms / 60ms）因为 4ms 的比例带而互相排除。

**Priority 优先于延迟。** 备用层存在的意义是上层不可用时接管，不是它恰好更快时接管；「便宜的主用 + 快但贵的备用」正是配置两层的常见理由。`TestPriorityOutranksLatency` 断言快 30 倍的低优先级渠道 200 次决策中一次都不该赢。

### 覆盖范围

`internal/proxy/failover_test.go`（17 个，含 5 个子测试）：状态矩阵、阶段/状态配对、成功留 NULL、首事件前 EOF、正常流交接且用量不重复计数、首事件后断流不可重试、首事件超时、闸门期客户端取消不扣健康分、`FailoverEligible=false` 保持旧行为、缓冲上限提交、纯心跳不算事件、无结尾空行仍算作答、401/403、429/502/529、闸门单元行为。

`internal/proxy/latency_test.go`（19 个）：最小样本门槛、未测量读作无数据、nil tracker 安全、未采样响应不记为快、样本过期、新旧样本混合、单个异常值不移动中位数、环容量与历史翻转、Reset、层内选快、容忍带保留、下限保护及其边界、冷启动回退加权、低样本仍参与、低样本不抖动、Priority 压过延迟、延迟只在层内排序、加权策略忽略延迟、默认策略为加权。

`internal/handlers/proxy_failover_test.go`（6 个，含 2 个子测试）：静默 SSE 渠道切换后由第二个渠道作答且客户端只收到一份完整答案、401/403 不跨渠道重试且第二渠道零命中、429 切换、两段选择优先未失败渠道（12 次重复消除随机性）、单渠道末次尝试语义、首事件后断流不切换（断言 `hello` **不**出现在已提交的 `half` 之后）。

`internal/handlers/upstream_routing_test.go`（6 个，含 8 个子测试）：规则常量发布、默认策略报告为未激活、区分「无样本」与「样本不足」、无 tracker 时空列表而非 panic、样本清除后不再报告、映射比较（新增/删除/改名/改值/清空/非法 JSON）。

### 新增 API 契约（前端接入点）

**1. 日志行新增两个字段**（`models.RequestLogOut`，分页列表、详情、实时 SSE 流三处都有）：

```json
{ "failure_stage": "first_event", "failure_retryable": true }
```

成功的行两者均为 `null`。

**2. 运行设置新增 `load_balance_strategy`**：取值 `"weighted"`（默认）或 `"least_latency"`。

> ⚠️ **`PUT /api/admin/settings` 整行替换。** `RuntimeSettingsIn` 里该字段留空会被规范化为 `"weighted"`——即把操作者设的 `least_latency` 悄悄改回默认。前端提交设置时必须带上当前值，否则保存别的设置项会重置策略。

**3. 新增 `GET /api/admin/upstreams/routing`**：

```json
{
  "strategy": "least_latency",
  "latency_active": true,
  "rules": { "min_samples": 5, "stale_window_seconds": 300, "sample_capacity": 32,
             "tolerance_ratio": 0.2, "tolerance_floor_ms": 50 },
  "latency": [ { "upstream_id": 9, "median_ms": 50, "sample_count": 12, "usable": true },
               { "upstream_id": 7, "median_ms": null, "sample_count": 2, "usable": false } ]
}
```

为什么是新端点而不是扩展 `AdminListUpstreams`：后者返回裸 JSON 数组，没有可以加字段的信封，给它加一个会破坏所有现有调用方。

三点使用约定：

- **`median_ms` 为 `null` 且 `sample_count > 0`** 是「正在采样（2/5）」，不是「无数据」。
- **未出现在 `latency` 数组里的渠道** 才是无数据。数组按 `upstream_id` 升序，轮询不会跳行。
- **`latency_active` 为 false 时** 不要展示延迟列：加权策略下样本照常采集，但不参与决策，展示它会让操作者以为路由在按延迟走。

不要用 Dashboard 的延迟图表数据替代这个端点：那些数字来自扫 `request_logs`，与路由实际依据的内存样本是两套口径，渠道卡片显示一个和决策不同的数字比不显示更糟。

### 验证命令

- `go build ./...`（通过）、`go vet ./...`（通过）、`gofmt` 新增文件已格式化
- `go test -race ./...`（全部通过）
- `go test ./internal/proxy/ -run 'Retryable|Failure|SSEHeader|SSEStreamProven|StreamFailing|GateTimes|ClientCancellationDuring|GateIsSkipped|GateCommits|CommentOnly|EventWithout|AwaitFirst|SuccessLeaves' -v`（17 通过）
- `go test ./internal/proxy/ -run 'Latency|Sample|Outlier|Ring|Reset|ColdStart|UnderSampled|LowSample|Priority|Weighted|Tolerance|Unmeasured|NilTracker|Unsampled|Expired|DefaultStrategy' -v`（19 通过）
- `go test ./internal/handlers/ -run 'Routing' -v`（5 通过）
- `go test ./internal/handlers/ ./internal/proxy/ ./internal/db/ ./internal/models/`（四包全绿）

### 交接事项

**前端需要接入（P1-1）**：`static/js/logs.js` 展示重试次数、渠道尝试链和失败阶段。重试链按 `request_uid` 分组、`attempt_index` 排序；`failure_stage` 建议给中文文案（`first_event` → 「首事件前失败」、`stream` → 「传输中断」、`client_cancelled` → 「客户端取消」等）；`failure_retryable` 不要渲染成「已重试」。

**前端需要接入（P1-2）**：`static/js/upstreams.js` 的策略选择读写运行设置的 `load_balance_strategy`，「当前策略与回退规则」和「无样本」提示读 `GET /api/admin/upstreams/routing`，不要自行计算路由结果，也不要复制规则常量。注意上面标 ⚠️ 的整行替换问题。

**后端仍未完成**：P2-1 Token 模型白名单、P2-2 模型定价与成本折算、P3 及之后全部。

**已知取舍**：

- 首事件超时/最大缓冲没有做成可配置项（理由见上表）。若产品需要，需同时补表单、校验、回显和路由端点的字段。
- 3xx / 400 / 404 仍计入渠道健康分，未改为中性（理由见上）。
- `LatencyTracker` 是纯内存状态，重启后从冷启动开始重新采样；这是有意的——重启前的延迟不描述重启后的渠道。渠道改 base URL 或模型映射时 `AdminUpdateUpstream` 会调 `Reset`（比较由 `sameModelMappings` 做，存储的 JSON 解析失败按「已变更」处理）；`AdminDeleteUpstream` 也会 `Reset`，因为 SQLite 会复用 id，否则新建渠道会带着前任的速度参与排序。

---

## 🔁 前端交接记录：P1-1 / P1-2 前端全量实施（2026-08-19）

### 任务

- [x] P1-1 前端：日志重试链、尝试序号、失败阶段标签与可重试错误状态
- [x] P1-2 前端：同层负载均衡策略选择、调度规则摘要条、渠道延迟标签与设置持久化

### 执行者

- 前端执行者（JavaScript / CSS / DOM / Node Tests）

### 修改文件

- `static/admin.html`：新增 `#log-detail-retry-chain` 重试链路容器、`#upstream-routing-summary` 调度摘要条、`#settings-load-balance-strategy` 策略选择框
- `static/css/enhancements.css`：新增重试链路步骤卡片、尝试序号徽标、失败阶段/可重试标签、调度摘要状态条、延迟标签样式
- `static/js/bootstrap.js`：注入 `settingsLoadBalanceStrategy` 等元素引用
- `static/js/logs.js`：
  - `formatLogChannelStack` 渲染重试序号徽标（`重试 #1`）
  - `formatLogDetailMeta` 渲染 `failure_stage`（中文映射如「首事件前失败」、「传输中断」等）与 `failure_retryable`（明确为「可重试错误/不可重试错误」，不误渲染为「已重试」）
  - `renderLogRetryChain` 按 `request_uid` 聚合同一请求各次尝试并按 `attempt_index` 排序，高亮当前节点，支持点击节点无缝切换查看各次尝试详情
- `static/js/upstreams.js`：
  - `fetchLatestRoutingData` 读取 `GET /api/admin/upstreams/routing`
  - `renderUpstreamRoutingSummary` 渲染同层调度策略、延迟激活状态、规则参数（最小样本、过期窗口、容忍带、样本容量）及 3 条回退规则说明，不自行计算路由结果
  - `formatLatencyRoutingBadge` 区分展示有效中位数延迟、采样中进度与未测量提示
  - 渠道表格与卡片集成延迟标签
- `static/js/shell.js`：`runtimeSettingsPayload` 纳入 `load_balance_strategy`，防止 PUT 全量覆盖丢失策略
- 新增测试：`tests/log-retry-failover.test.mjs`（7 组测试）、`tests/upstream-routing-strategy.test.mjs`（7 组测试）

### 验证命令

- `node --test tests/*.test.mjs`（156 全部通过）
- `go vet ./...`（通过）
- `go test ./...`（全部通过）

### 交接事项

- **P1 完成状态**：P1-1（SSE 首字前故障转移）与 P1-2（Least-Latency 调度）前后端均已 100% 完成，所有相关测试就位。
- **未完成项**：P2-1（Token 模型白名单）、P2-2（模型定价与成本折算）、P2-3（周期性配额）及 P3 监控与灾备系列任务待后续实施。


---

## 🔁 后端交接记录：P2-1 / P2-2 / P2-3 全量实施（2026-08-19）

> **本记录中 P2-2 的部分已失效**：定价与费用估算已于 2026-08-20 整体移除，下文提到的 `internal/{models,db,proxy,handlers}/pricing.go` 与 `model_prices` 表均已不存在。保留原文仅为留痕当时的设计取舍。

### 任务

- [x] P2-1 Token 模型白名单（后端）
- [x] P2-2 模型定价与成本折算（后端）
- [x] P2-3 周期性配额自动重置（后端）

### 执行者

- 后端执行者（Go / SQLite / API）

### 修改文件

新增：`internal/models/modelpolicy.go`、`internal/models/pricing.go`、`internal/models/quotaperiod.go`、`internal/db/pricing.go`、`internal/db/quotaperiod.go`、`internal/proxy/pricing.go`、`internal/handlers/pricing.go`

新增测试：`internal/models/{modelpolicy,pricing,quotaperiod}_test.go`、`internal/db/{token_allowed_models,pricing,quotaperiod,token_quota_cycle}_test.go`、`internal/proxy/pricing_settlement_test.go`、`internal/handlers/{proxy_model_whitelist,proxy_pricing}_test.go`、`internal/middleware/quota_period_test.go`

改动：`internal/db/{sql,schema,token,log}.go`、`internal/models/{token,requestlog}.go`、`internal/middleware/auth.go`、`internal/proxy/{logging,client}.go`、`internal/quota/tracker.go`、`internal/handlers/proxy.go`、`internal/app/{router,server}.go`、`internal/appstate/state.go`

顺带修复：`internal/handlers/upstream_detail_test.go` 的 `itoa` 原为 `string([]byte{byte('0'+value)})`，只对 0-9 正确，超出后静默产出垃圾字节。我的测试传 1000000 后 JSON 变成非法，报出来的却是"用量缺失"——名字像标准库却在第 10 个值起悄悄骗人。

---

### P2-1：白名单匹配刻意比渠道匹配更严

`modelMatchScore`（选渠道用）除精确匹配外还接受请求是渠道模型名的**前缀或后缀**。那是选路时的便利，用作授权判断就是漏洞：

- 后缀方向：限制为 `gpt-4o-mini` 的令牌会放行 `mini`
- 前缀方向：限制为 `gpt-4o` 的令牌会放行 `gpt-4o-anything`

所以 `AllowedModelsPolicy.Permits` 只做**精确（大小写无关）**与**显式尾部 `*`**，不做任何隐式模糊匹配。`TestWhitelistDoesNotInheritTheChannelMatchersFuzziness` 三个方向都钉住了。

内部 `*`（如 `gpt-*-turbo`）在写入时拒绝，而不是存成一条永不匹配却看起来在生效的规则。

### 空值语义（清单要求"唯一且明确"）

NULL 与 `[]` 都表示"不限制"，**写入只产出 `[]` 一种拼写**，读取把 NULL 折叠进去。裸 `*` 也归一为不限制——否则 `Unrestricted()` 返回 false 而 `Permits` 全放行，控制台会把它标成"受限"并给它单独的缓存指纹，效果却与没有白名单完全相同。

**受限令牌未指定模型时拒绝（fail-closed）**。反向选择会让任何读不出模型的请求绕过白名单。不限制的令牌不受影响，所以这条严格性只作用于运营者刻意收窄过的凭证。

### 校验时机：客户端模型，不是映射后模型

强制点在 `ProxyHandler` 解析 body 之后、选渠道之前。渠道的 `model_mappings` 无法把模型洗白过去，且运营者写白名单时想的就是客户端会发什么。`TestTheWhitelistIsJudgedOnTheClientsModelNotTheMappedOne` 用"白名单只列上游侧 id、客户端发映射键"这个组合断言必须 403。

### `/v1/models` 缓存键含策略指纹

清单要求"缓存键必须包含策略版本"。实现为 `ModelsCacheKey{GroupID, PolicyFingerprint}`，指纹由归一化后的匹配集合构造（重排、改大小写不改变指纹）。

这样选的理由：**令牌写入路径目前完全不失效这个缓存**。做成"版本号 + 编辑时 bump"需要在每个令牌写入点加失效调用，漏一个就会长期发错列表；指纹让编辑后的令牌自动落到另一个键上，不需要任何失效步骤。`TestEditingAWhitelistTakesEffectWithoutInvalidatingTheCache` 直接改库不做任何失效，断言放宽和收窄都立即生效。

不限制的令牌指纹为空，与分组共享一个条目，所以常见情况没有额外内存。

### 拒绝响应

403（不是 400）：请求本身合法、模型可能真实存在，是这个凭证无权使用。两种方言 + 顶层 `code`（`MODEL_NOT_ALLOWED`），Anthropic 侧用 `permission_error`，OpenAI 侧 `error.code = model_not_allowed`。两种形状 SDK 都不会重试，这对一个不会自行改变的拒绝是正确行为。拒绝会写日志行（403 + 无渠道），否则运营者看不到有人在拿受限 key 试模型。

---

### P2-2：金额全整数，无任何 float64

存储单位是**货币主单位的百万分之一（micro-unit）**，单价按每百万 token 报价——这也是供应商价格表的形状，所以存进去的数字运营者能直接对账。

清单要求"禁止用二进制浮点累计账单"。`internal/models/pricing.go` 里没有 float64。2.50 美元是 `2_500_000` micro-USD，精确；作为 float64 不精确。

**逐维度四舍五入后求和**，不是求和后舍入一次。后者会让每个维度留下一个在存储金额里无表示的小数余项，于是总额不等于控制台并列显示的四个分项之和。半值向上（不是银行家舍入），因为运营者手算核对时用的是这个规则。

### 缓存 token 不重复计费

供应商把 cached tokens 报成 `prompt_tokens` 的**子集**，不是额外量。按 `prompt_tokens` 全额计费再加上 cache-read 会把缓存部分收两次费（一次贵、一次便宜），正好把缓存本该变便宜的场景变贵。

`billableTokens` 先做 `prompt - cached`（下限 0，供应商报出 cached > prompt 时不给客户返利），再分别按新鲜价和缓存价计。`TestCachedPromptTokensAreNotBilledTwice` 断言 1.90 美元并显式点出错误答案 10.90 美元。

### 结算时机在 `Schedule()`，不在 flush

`LogWriter.Schedule` 是请求完成、用量已知那一刻。在那里定价，金额就对"入队到落库之间的价格编辑"免疫——队列积压时这个窗口可能有几秒。在 `flush` 定价则会让那段时间内的价格改动改写已服务请求的金额。

价格表只增不改（无 update 端点），改价是插入一条更晚 `effective_from` 的新版本。请求按**运行时生效的版本**定价，所以三个月后还能解释一个金额。`TestTheCostSnapshotSurvivesAPriceEdit` 先结算再改价，断言已写入行不动。

### 三列同生同灭

```text
cost_micros      INTEGER 或 null
cost_currency    TEXT 或 null
pricing_rule_id  INTEGER 或 null
```

三者**同时为 NULL** 表示"该模型无价格规则" = **成本未知**，与"规则定价为 0" = **免费** 严格区分。前端不得把 NULL 渲染成 0，否则看板求和会静默漏掉定价前的全部历史请求和所有未定价模型的真实支出。

`pricing_rule_id` **不设外键**：删掉一个价格版本不能擦除历史请求的计费记录。`TestDeletingAPriceVersionLeavesSettledAmountsIntact` 钉住了这点。

### 规则选择是全序的

匹配模型且已生效的规则中：先按**模式具体度**（精确 > 长通配 > 短通配 > `*`），再按 `effective_from` 更晚，再按 id 更大。没有这个排序，加一条兜底 `*` 会与每条具体规则竞争，胜者取决于扫描顺序——一张改一行就变义的价格表。`TestAnExactRuleOutranksAWildcardThatAlsoMatches` 用三种排列断言同一结论。

定价匹配同样**不用**渠道匹配的模糊规则：`gpt-4o` 的规则不能给 `gpt-4o-mini` 定价，否则小模型按大模型收费。

### 新增 API

```text
GET    /api/admin/pricing        列出全部版本（含历史），并回传单位约定
POST   /api/admin/pricing        新增一个版本
DELETE /api/admin/pricing/{id}   删除一个版本
```

列表响应带 `token_basis` / `micro_units_per_unit` / `currencies`，**前端不要自己抄一份单位常量**：除错的一方会把价格显示成一百万倍偏差，而两边都没有线索说明谁错了。

---

### P2-3：无定时任务，滚动由新周期的第一个请求原子完成

清单明确禁止"简单 `used_tokens = 0`"。实现是：token 行存 `quota_period_key`（实际是 period **stamp**，见下），用量由一条 UPDATE 施加，语句自己判断来件属于哪个周期：

| 来件 stamp 相对行内 stamp | 行为 |
| :--- | :--- |
| 相同 | 累加（常规情况） |
| 同类型、更晚 | **这是新周期的第一笔**：计数器从这笔用量重新开始，行采用新 stamp |
| 同类型、更早 | 已关闭周期的晚到用量，**丢弃金额**，不污染新周期 |
| 类型不同 | 两个 stamp 不可比，累加并采用新 stamp（见下） |

滚动因此与"记录触发它的那个请求"是同一条语句，不存在"计数器已清零但清零它的请求还没记账"的窗口。也不需要重启恢复：stamp 是从 `quota_period` + `quota_timezone` + 时刻**推导**的，任何进程任何时候对同一时刻都算出同一个值。generation 计数器就需要恰好一方恰好推进一次。

### 为什么是 stamp 而不是裸日历键

**这是测试逼出来的一个真实设计缺陷。** 我最初存"周期类型"和"裸日历键"两个字段，SQL 先比类型再比键。但运营者改周期类型后，**行内类型已经等于来件类型**，比较就落到不可比的键形状上：

- 日→月：行内 `2026-08-19`，来件 `2026-08`，字典序来件更小 → 判为晚到 → **静默丢弃真实用量**
- 月→日：行内 `2026-08`，来件 `2026-08-19`，来件更大 → 判为新周期 → **白送已花掉的额度**

改为自描述 stamp `daily:2026-08-19` / `monthly:2026-08`，SQL 用 `substr(x, 1, instr(x, ':'))` 比较冒号前缀判定可比性。`TestChangingThePeriodTypeDoesNotWipeOrDropUsage` 两个方向都钉住。

`QuotaPeriodKey` 仍返回裸日历键供控制台展示；存储与比较用 `QuotaPeriodStamp`。

### 边界落在令牌自己的时区

stamp 形状（均在令牌时区内计算）：

```text
none     ""                一个无尽周期
daily    daily:2026-08-19
weekly   weekly:2026-W34   ISO 周（补零，所以字典序即时间序）
monthly  monthly:2026-08
```

默认 UTC 而非宿主本地时区：边界随部署区域漂移会让配额提前或延后重置，运营者无从察觉。

周期起止用**日历字段构造**，不是 `Truncate`——后者作用于绝对时间，在任何非整小时偏移的时区会落在错误时刻，且完全忽略 DST。

**夏令时**：`TestDaylightSavingDoesNotSkipOrRepeatAPeriod` 覆盖 2026-03-08（23 小时日）与 2026-11-01（25 小时日），断言那一天的每个小时都属于同一 stamp、周期长度确实是 23/25 小时、且下个周期的起点恰好等于本周期的终点。`TestConsecutivePeriodsTileWithoutGapsOrOverlaps` 把这条性质推广到三种周期各 60 个连续周期。

**月末**：31/30/28/29 天、12 月跨年都覆盖；周期起点始终是 1 号，永不落在只有 30 天的月份的 31 号。

### 在途用量也按周期隔离

`quota.Tracker` 的键从 `tokenID` 改为 `{tokenID, periodStamp}`。否则边界前取的 hold 会继续压在边界后的预算上：午夜繁忙的令牌会在预算刚补满的那一刻被拒。释放也按**取 hold 时的那个周期**，否则已关闭周期的 hold 永远挂着，永久缩小它作用的那份预算。

关闭周期的条目在最后一个请求结算后被删除，这也是 map 跨边界的上界。

### 准入读的是"当前周期的用量"

计数器由新周期第一笔用量清零，**不是在边界那一刻清零**。所以边界之后到那一笔之前，行里还是旧数字。直接拿它做准入会拒掉本该执行滚动的那个请求——令牌会永久卡在耗尽状态，因为能清零它的请求永远跑不了。

`DownstreamCredential.UsedTokensInCurrentPeriod` 与 `APITokenRow.UsedTokensNow` 是同一逻辑的两份（准入侧与控制台侧），**刻意保持一致**：两边不能对"已花多少"给出不同答案。配额拒绝文案也报当前周期的用量，不是行内原始总数。

`UpdateToken` **不触碰** `quota_period_key`：它记录的是已存用量属于哪个周期，施加语句需要它原样保留才能区分"周期类型变更"与"周期滚动"。编辑时重写它会导致丢用量或白送额度。

### 新增 API 契约（前端接入点）

`APITokenIn` / `APITokenUpdateIn` 新增：

```text
allowed_models   string[]   缺省/null/[] 均为不限制
quota_period     string     none | daily | weekly | monthly，缺省 none
quota_timezone   string     IANA 名，缺省 UTC
```

三者都是**整体替换**语义（像 `expires_at`，不像 `token`）：空数组清除白名单，缺省 period 关闭自动重置。无法识别的 period **返回 400 而非静默默认**——否则控制台发了 `monthy`，会存成永不重置却在表单上显示月度。

`APITokenOut` / `APITokenCreatedOut` 新增：

```text
allowed_models      string[]           永远是数组，不会是 null
quota_period_state  {
  period, timezone, period_key,
  period_start, period_end, next_reset_at   // none 时后三者为 null
}
```

`quota.used_tokens` 现在是**当前周期**的用量。`next_reset_at` 由服务端推导，**前端不要自己算边界**：另一个时区的浏览器会显示错误的重置时刻，时钟有偏差的浏览器会显示网关并未使用的周期。

日志行新增（列表 / 详情 / 实时流三处一致）：

```text
cost_micros      int64 或 null
cost_currency    string 或 null
pricing_rule_id  int64 或 null
```

### 验证命令

- `go build ./...`（通过）、`go vet ./...`（通过）、新增文件均已 `gofmt`
- `go test ./... -count=1`（全部通过）
- `node --test tests/*.test.mjs`（156 全部通过，未回归）
- `go test ./internal/models -run 'Whitelist|Wildcard|Fingerprint|Cost|Cached|Rounds|Priced|FormatMicros|Period|Daylight|Consecutive' -v`
- `go test ./internal/db -run 'AllowedModels|Pricing|CostColumns|UsageAccumulates|FirstUsageOfANewPeriod|LateArriving|ChangingThePeriod|QuotaCycle|ConcurrentUsage' -v`
- `go test ./internal/handlers -run 'Whitelist|ModelsList|CostSnapshot|CachedRequest|PriceEdit' -v`
- `go test ./internal/middleware -run 'AdmissionIgnores|OutstandingHolds|StragglersHold' -v`

### 交接事项

**前端需要接入（P2-1）**：令牌弹窗的模型多选与快捷输入；空数组的"不限制"展示语义；403 的 `MODEL_NOT_ALLOWED` 提示。注意白名单只支持精确与尾部 `*`，UI 不要暗示支持内部通配。

**前端需要接入（P2-2）**：价格表管理界面（列表 / 新增 / 删除，**没有编辑**——改价是新增版本，UI 应体现这点）；日志详情与看板显示估算金额、币种、价格版本；金额三列为 null 时显示"未定价"而**不是** 0.00；文案需明确"估算金额，不等同于供应商最终账单"。单位常量从 `GET /api/admin/pricing` 读，不要在前端硬编码。

**前端需要接入（P2-3）**：令牌弹窗的每日/每周/每月周期与时区选择；展示 `quota_period_state` 的当前周期、已用/上限、下次重置时间与时区，全部直接用服务端字段。

**后端仍未完成**：

- **看板金额聚合未做**：清单 P2-2 提到"扩展 Dashboard 聚合"，本轮只做了日志行级金额。看板按周期求和金额需要新的聚合查询，且必须处理"部分请求未定价"——把 NULL 当 0 求和会给出一个看起来精确却偏低的总额。建议聚合同时回传"已定价请求数 / 总请求数"，让前端能标注覆盖率。
- P3 全部（Prometheus、配置迁移、SQLite 灾备）。

**已知平台限制**：

- **`go test -race` 在本机不可用**：`go env CC` 指向 gcc，但 gcc 既不在 PATH 也不在常见安装位置，`CGO_ENABLED=1 go test -race` 报 `cgo: C compiler "gcc" not found`。**上一轮 P0-B3 交接记录声称"gcc 13.2.0 在、race 全包通过、此前的限制已不成立"，该结论现已不成立**，应视为过期。
- 本轮**确实引入了新的并发结构**：`quota.Tracker` 的 map 键从 `int64` 改为结构体、`proxy.PricingBook` 新增读写锁。`TestConcurrentSettlementAndReplaceDoNotRace` 与 `TestConcurrentUsageAppliesEveryAmountExactlyOnce` 在无 race 检测下通过（后者靠 `SetMaxOpenConns(1)` 与单条 UPDATE 的原子性，能检出丢失更新），但**锁的正确性需要在装有 gcc/clang 的环境补跑** `go test -race ./internal/quota ./internal/proxy ./internal/db ./internal/middleware`。
- 价格表加载失败时 `PricingBook` 为空，服务照常代理但不估算成本（成本列全 NULL）。这是有意的降级方向：定价读不出来不该让网关停摆。


---

## 🔁 前端交接记录：P2-1 / P2-2 / P2-3 前端全量实施（2026-08-19）

> **本记录中 P2-2 的部分已失效**：定价界面、日志费用徽标与成本卡片已于 2026-08-20 整体移除，下文提到的 `static/js/pricing.js`、`#pricing-*` 元素与 `.log-*-cost-*` 样式均已不存在。

### 任务

- [x] P2-1 前端：令牌模型白名单输入与解析校验、表格白名单标签与全部模型展示
- [x] P2-2 前端：版本化价格表管理界面（列表/新增版本/删除）、日志行预估金额徽标、日志详情成本卡片与供应商账单免责声明、高精度单价微单位换算与格式化
- [x] P2-3 前端：令牌周期性配额（每日/每周/每月）与 IANA 时区选择、当前周期用量/上限/重置时间展示

### 执行者

- 前端执行者（JavaScript / CSS / DOM / Node Tests）

### 修改文件

- `static/admin.html`：
  - 令牌表格新增「允许模型」表头与列定义（总列数更新为 9 列）
  - 令牌弹窗新增 `#token-allowed-models`（多模型与尾部通配输入）、`#token-quota-period`（配额重置周期选择）、`#token-quota-timezone`（重置时区输入）
  - 设置页新增 `#pricing-settings-title`「模型定价规则」配置卡片与表格 `#pricing-rule-rows`
  - 新增 `#pricing-rule-dialog` 定价规则弹窗与表单 `#pricing-rule-form`
  - 引入 `/static/js/pricing.js` 脚本
- `static/css/enhancements.css`：
  - 新增令牌白名单标签 `.token-models-cell` / `.token-model-tag` / `.token-model-more`
  - 新增周期配额徽标 `.quota-period-badge`
  - 新增定价规则表格与币种徽标 `.pricing-table-wrap` / `.pricing-model-code` / `.badge-wildcard` / `.badge-usd` / `.badge-cny`
  - 新增日志行费用徽标 `.log-token-cost-badge`、日志详情成本卡片 `.log-detail-cost-card` / `.log-detail-cost-amount` / `.log-cost-disclaimer`
- `static/js/bootstrap.js`：注入定价规则相关 DOM 元素与 `tokens` / `pricing` 状态引用
- `static/js/pricing.js`（新增）：
  - `loadPricingRules()` 加载规则与单位约定（`GET /api/admin/pricing`）
  - `renderPricingRules()` 渲染版本化价格表，显示精确单价与生效时间
  - `unitPriceToMicros()` 与 `formatPricingRate()` 严格整数 BigInt 换算，无浮点误差
  - `handlePricingRuleSubmit()` 校验并提交新增规则（`POST /api/admin/pricing`）
  - `handlePricingTableAction()` 带确认弹窗删除定价版本（`DELETE /api/admin/pricing/{id}`）
- `static/js/tokens.js`：
  - `parseAllowedModelsInput()` 解析并校验白名单（支持逗号/换行、尾部 `*` 通配符、去重、拒绝内部通配）
  - `allowedModelsCellMarkup()` 渲染模型标签、`+N` 溢出徽标或「全部模型」
  - `quotaCellMarkup()` 集成周期类型徽标（每日/每周/每月）与服务端推导的下次重置时间提示
  - `openTokenDialog()` / `editToken()` / `tokenForm.submit` 全量支持白名单与配额周期/时区
- `static/js/logs.js`：
  - `formatLogCostText()` 格式化单条请求金额，null/无效值严格返回「未定价」，严禁渲染为 `$0.00`
  - `formatTokens()` 渲染费用徽标
  - `formatLogDetailMeta()` 渲染预估费用卡片、定价版本号与「估算金额，不等同于供应商最终账单」免责提示
- `static/js/shell.js`：进入设置页时触发 `loadPricingRules()`
- 测试文件：
  - `tests/pricing-and-whitelist.test.mjs`（8 组测试全部就位）
  - 更新 `tests/token-expiry.test.mjs`（列数契约更新为 9 列）
  - 更新 `tests/log-retry-failover.test.mjs` / `tests/logs-filters.test.mjs`（补充成本上下文适配）

### 验证命令

- `node --test tests/*.test.mjs`（164 组测试全部通过）
- `go vet ./...`（全部通过）
- `go build ./...`（编译正常）
- `go test ./internal/models ./internal/db ./internal/handlers ./internal/proxy ./internal/middleware ./internal/quota`（全部通过）

### 交接与完成状态

- **P1 完成状态**：P1-1（SSE 首字前故障转移）与 P1-2（Least-Latency 调度）前后端均已 100% 落地。
- **P2 完成状态**：P2-1（Token 模型白名单）、P2-2（模型定价与成本折算）、P2-3（周期性配额）前后端均已 100% 落地。
- **后续未完成项**：P3 系列任务（Prometheus `/metrics` 端点、配置迁移包、SQLite 灾备）。
  - 该结论已过期：P3 三项均已完成，见下方三份交接记录（2026-08-20）。

---

## 🔁 交接记录：P3-1 Prometheus /metrics 端点（2026-08-20）

### 任务

- [x] 后端（上一次提交 88e6c1f 已落地，本轮仅校准勾选状态）：`/metrics` 端点、有界标签、默认关闭、独立凭据
- [x] 前端：设置页展示端点开关状态、访问路径与凭据要求，不提供开关

### 执行者

- 后端部分：上一次提交（88e6c1f）；勾选校准与前端部分：本轮执行者

### 修改文件

- `internal/metrics/prometheus.go` / `exposition.go`（上一次提交）：指标注册与文本曝光格式
- `internal/handlers/admin.go`：新增 `metricsEndpointStatus()`，把端点状态并入 `GET /api/admin/system`
- `internal/models/settings.go`：新增 `MetricsEndpointStatusOut`（`enabled` / `path` / `token_required` / `configured_by_file`）
- `internal/handlers/metrics_status_test.go`（新增）：状态输出不泄漏凭据本身
- `static/admin.html`：新增只读卡片 `#metrics-endpoint-status`
- `static/js/bootstrap.js` / `static/js/shell.js`：注入 `metricsEndpointStatusEl` 并在 `renderSystemInfo()` 渲染
- `tests/metrics-endpoint-status.test.mjs`（新增）

### API 契约

- `GET /metrics`：Prometheus 文本格式；默认关闭；启用后由独立于管理员令牌的凭据保护
- `GET /api/admin/system` 响应新增 `metrics_endpoint` 对象；**只返回是否要求凭据（`token_required` 布尔），不返回凭据值**

### 验证命令

- `go vet ./...`
- `go test ./internal/metrics ./internal/handlers`
- `node --test tests/metrics-endpoint-status.test.mjs`

### 交接事项

- 标签基数已按清单约束收敛：**不含令牌 ID、令牌名称与任意模型字符串**，只使用有界枚举维度。
- 端点开关只走启动配置，控制台故意不提供写入入口，避免通过管理界面把监控端点暴露出去。
- 前端在端点关闭时展示“未启用”，启用但无凭据时展示明确告警文案，与后端 `slog.Warn` 一致。

---

## 🔁 交接记录：P3-2 配置迁移包（2026-08-20）

### 任务

- [x] 后端：可移植配置归档导出/导入、AEAD 加密、dry-run 预览、冲突策略
- [x] 前端：导出下载、导入上传、预览差异展示与冲突策略选择

### 执行者

- 本轮执行者（后端 Go + 前端 JavaScript）

### 修改文件

- `internal/models/configarchive.go`（新增）：归档头、条目与请求/响应模型
- `internal/configpack/configpack.go`（新增）：容器编解码、AES-256-GCM + argon2id 加密、头部作为 AAD 绑定
- `internal/db/configmigrate.go`（新增）：按名称匹配的导出与单事务导入，dry-run 与实写共用同一套差异计算
- `internal/handlers/configmigrate.go`（新增）：`POST /api/admin/config/export` 与 `/import`
- `internal/app/router.go`：挂载 `/api/admin/config` 路由组
- `internal/configpack/configpack_test.go` / `internal/db/configmigrate_test.go` / `internal/handlers/configmigrate_test.go`（新增）
- `static/admin.html`：新增「配置迁移」卡片（导出面板 + 导入面板 + 预览报告）
- `static/js/bootstrap.js` / `events.js` / `shell.js`：DOM 引用、监听与迁移逻辑
- `static/css/enhancements.css`：`.config-migrate*` 相关样式
- `tests/config-migration.test.mjs`（新增）

### API 契约

- `POST /api/admin/config/export`：请求体 `{scopes, include_secrets, password}`；响应为 JSON 归档文档（`kind` / `schema_version` / `app_version` / `exported_at` / `scopes` / `includes_secrets` / `checksum`，加上 `payload` 与 `encryption` 二者之一）。用 POST 而非 GET，因为请求体带密码，query 里的密码会进访问日志与浏览器历史。
- `POST /api/admin/config/import`：请求体 `{archive, password, on_conflict, dry_run, scopes}`；`on_conflict ∈ {skip, overwrite, fail}`，留空即 `skip`
- dry-run 与实写走同一段代码，返回同一份报告结构（新增/更新/跳过/冲突逐条列出）；任何拒绝都整体回滚并以 400 携带完整报告返回
- 兼容性由 `schema_version` 把关，`app_version` 只记录不校验——按补丁号拒绝会让两个本来兼容的实例无法互相迁移

### 验证命令

- `go vet ./...`
- `go test ./internal/configpack ./internal/db ./internal/handlers`
- `node --test tests/config-migration.test.mjs`

### 交接事项

- **开发中发现并规避的陷阱**（同属本轮新代码，非既有缺陷）：`fail` 冲突策略若按字面实现，会因“默认分组”这一每个实例必然存在的同名记录而拒绝任何全量归档，使该策略实际不可用。`resolveConflict()` 因此接受一个 `matches` 参数：存量定义与归档要求一致时按幂等跳过，而不是当成冲突失败。
- 密码派生使用随机 per-file salt，读侧对文件自述的 argon2 代价参数设上界，防止一个被改写的头部让服务替攻击者做无界计算。
- 敏感配置一律走 AEAD，**不存在明文落盘路径**；前端密码框 `type="password"`，不回显、不写 `localStorage`。
- 归档按**名称**匹配而非主键，因此可跨实例迁移；这是有意选择，代价是名称本身成为迁移身份。

---

## 🔁 交接记录：P3-3 SQLite 一致性全量备份与恢复（2026-08-20）

### 任务

- [x] 后端：`VACUUM INTO` 一致性快照、结构指纹、加密容器、两阶段恢复与回滚副本
- [x] 前端：备份下载、恢复上传、元信息展示与明确的覆盖确认
- [x] 前端：恢复期间锁定相关操作，并明确是否需要重启
- [x] 前端：错误密码、损坏文件、取消恢复与成功恢复的 UI 测试

### 执行者

- 本轮执行者（后端 Go + 前端 JavaScript）

### 修改文件

- `internal/models/backup.go`（新增）：容器头、加密参数、备份信息与恢复报告模型
- `internal/backup/snapshot.go`（新增）：`VACUUM INTO` 快照、`PRAGMA quick_check`、结构指纹
- `internal/backup/container.go`（新增）：长度前缀二进制容器、AES-256-GCM + argon2id、`deriveKey()`
- `internal/backup/restore.go`（新增）：暂存 + 标记文件，下次启动在连接池打开前原子接管，保留回滚副本
- `internal/handlers/backup.go`（新增）：信息 / 备份 / 恢复 / 取消四个处理器
- `internal/app/router.go`：挂载 `/api/admin/disaster-recovery` 路由组
- `internal/app/server.go`：启动时在开池之前检查并接管暂存的恢复
- `internal/appstate/state.go`：暂存状态与数据库路径
- `internal/backup/backup_test.go`（新增，23 组测试）
- `static/admin.html`：新增「备份与恢复」高危卡片（暂存横幅 + 备份面板 + 恢复面板）
- `static/js/bootstrap.js`：新增 15 个 DOM 引用
- `static/js/events.js`：备份/校验/恢复/取消/输入变更监听
- `static/js/shell.js`：`api()` 拆为 `rawApi()` + `api()`、`readBackupFile()`、`bytesToBase64()`、`runDatabaseBackup()`、`submitRestore()`、`cancelStagedRestore()`、`applyDisasterRecoveryLocks()`、`loadDisasterRecoveryInfo()`
- `static/css/enhancements.css`：`.restore-pending` / `.backup-info` / `.restore-report` 等样式
- `tests/disaster-recovery.test.mjs`（新增，26 组测试）

### API 契约

- `GET /api/admin/disaster-recovery/info`：响应 `{current, pending_restore}`；`current` 为大小/页大小/页数/结构指纹，`pending_restore` 为 `{pending, staged_at, size_bytes, checksum}`
- `POST /api/admin/disaster-recovery/backup`：请求体 `{password}`（可选，留空即不加密；非空则至少 8 字符）；响应为 `application/octet-stream` 字节流，带 `content-disposition` 文件名 `wildtoken-<UTC 时间戳>.wtbak`、`cache-control: no-store` 与 `x-backup-checksum`
- `POST /api/admin/disaster-recovery/restore`：请求体 `{archive(base64), password, dry_run, allow_schema_mismatch}`，实写时**必须**附带 `confirm: "restore"`；响应 `{verified, backup, current, compatible, incompatibility, staged, requires_restart, rollback_path, warnings}`
- `DELETE /api/admin/disaster-recovery/restore`：取消暂存的恢复，响应 `{cancelled, rollback_kept}`
- 容器格式：`magic(8) + hdrLen(4 BE) + JSON header + body`，头部作为 AEAD 的 AAD 绑定
- 备份与恢复同样用 POST 而非 GET：请求体带密码，且备份不应该能被一个链接触发

### 验证命令

- `go vet ./...`
- `go test ./...`（全部包 `ok`，含 `internal/backup` 15.7s）
- `node --test tests/*.test.mjs`（218 组测试全部通过）

### 交接事项

- **与清单的偏差**：清单建议放在 `internal/db/backup.go`，实际落在独立包 `internal/backup/`（`snapshot.go` / `container.go` / `restore.go`）加 `internal/handlers/backup.go`。理由是恢复逻辑必须在连接池打开之前运行，若放进 `internal/db` 会与该包“已持有连接”的前提互相矛盾。
- **恢复需要重启**：Windows 无法替换已打开的文件，因此恢复是两阶段的——运行中只暂存文件与标记，下次启动在开池前原子接管，并清理陈旧的 `-wal` / `-shm`。该要求同时写在状态行和常驻报告块里。
- **暂存期间连配置导入一起锁定**：在暂存与重启之间写入的配置导入会落进下次启动即被替换的数据库，等于静默丢失。
- **修复的产品缺陷**：`deriveKey()` 此前对头部中为零的 argon2 参数回退到构建默认值，既绕过了敌意头部上界，又会用不同的密钥派生结果把**正确的密码**报成错误。现改为四个参数缺一即拒绝。
- 前端在上传前先本地解析容器头（magic、长度上界、`kind`），因此选错文件（例如把配置迁移包当备份）会在输入密码和上传整库之前被拒绝；服务端仍完整复核一遍。
- 校验通过后暂存的字节与预览时的字节是同一份（持有 `stagedBackupFile`），避免二次点击重新编码整库，也避免“校验的和写入的不是同一个文件”。
- 恢复失败时状态文案明确追加“（当前数据库未被修改）”，避免操作者在失败后不确定实例状态。

---

## 🗑️ 费用估算功能移除记录（2026-08-20）

### 任务

- [x] 移除后端结算链路、价格表存取、`/api/admin/pricing` 全部端点与 `model_prices` 表
- [x] 移除前端定价管理界面、日志行费用徽标、日志详情费用卡片与免责声明
- [x] 配置迁移包降去 `pricing` scope，`schema_version` 升至 2
- [x] 清单条目改判为作废（P2-2），历史交接记录标注失效范围

### 为什么整条移除，而不是补完看板金额聚合

上一轮的遗留项是"看板金额聚合未做"。补完它需要一张长期正确的价格表，而这个网关不产生账单：价格由各供应商随时调整，运营者若不同步维护，看板给出的就是一个**看起来精确、实际偏差**的总额——它比"不显示金额"更容易被当成对账依据。运营者据此决定放弃整条功能链，而不是把一个需要持续人工维护才能不出错的数字留在界面上。

### 修改文件

删除：`internal/models/pricing.go`、`internal/db/pricing.go`、`internal/proxy/pricing.go`、`internal/handlers/pricing.go`、`static/js/pricing.js` 及对应测试 `internal/models/pricing_test.go`、`internal/db/pricing_test.go`、`internal/proxy/pricing_settlement_test.go`、`internal/handlers/proxy_pricing_test.go`、`tests/pricing-and-whitelist.test.mjs`

修改（后端）：`internal/proxy/logging.go`（`LogEntry.Cost` 与 `LogWriter.pricing` 去除，`NewLogWriter` 少一参，INSERT 由 33 占位符降为 30）、`internal/appstate/state.go`、`internal/app/server.go`、`internal/app/router.go`、`internal/models/requestlog.go`、`internal/db/log.go`、`internal/db/sql.go`、`internal/db/schema.go`、`internal/models/configarchive.go`、`internal/db/configmigrate.go`、`internal/handlers/configmigrate.go`

修改（前端）：`static/admin.html`（定价卡片、弹窗、`pricing.js` 脚本标签、导出 scope 勾选框）、`static/js/bootstrap.js`（22 行 DOM 引用与状态）、`static/js/shell.js`（`loadPricingRules()` 调用与 scope 文案）、`static/js/logs.js`（`formatLogCostText()`、费用徽标、费用卡片）、`static/css/enhancements.css`（定价表与费用卡片样式）。`static/js/events.js` **无需改动**：定价的监听器原本就写在 `pricing.js` 内部，随文件一并消失

新增测试：`tests/whitelist-and-quota.test.mjs`（承接原文件中白名单与配额部分，并新增"控制台不残留任何费用痕迹"断言）、`internal/configpack/configpack_test.go` 的 `TestAnArchiveCarryingRetiredPricesSaysSo`

### 两个需要运营者决策、已确认的取舍

**1. 删表，但保留 `request_logs` 的三列**

`model_prices` 表整体 `DROP`（索引先于表，避免留下指向不存在表的索引）。但 `request_logs.cost_micros` / `cost_currency` / `pricing_rule_id` **保留为退役列**：SQLite 删列需要重建整表，而 `request_logs` 是库里最大的表；且已结算的历史金额就存在这三列里。`ensureColumn` 对这三列的调用**必须保留**——P3-3 的备份用结构指纹判断兼容性，若只让新装实例缺列，升级实例与新装实例的快照会因为几个没人读的列而互相"不兼容"。

**2. v1 归档带 `pricing` scope 时拒绝，而不是静默跳过**

`ConfigArchiveSchemaVersion` 升到 2，`ConfigScopePricing` 常量保留（不在 `ConfigScopes` 里，因此永不导出、永不接受、永不应用），仅用于识别并**拒绝**：归档声明自己带价格，把其余部分导入而悄悄丢掉价格，正是"运营者以为迁移完整"的那种情况。错误文案直接说明该功能已移除，并提示从源实例重新导出。

### 验证命令

- `go vet ./...`（无输出）
- `go test ./...`（全部包 `ok`）
- `CGO_ENABLED=1 go test -race ./internal/db ./internal/handlers ./internal/proxy ./internal/quota ./internal/middleware ./internal/configpack ./internal/backup`（全部通过）
- `node --test "tests/*.test.mjs"`（215 组测试全部通过）

### 交接事项

- **`NewLogWriter` 签名变更**：少了 `pricing *PricingBook` 参数，四处测试调用点已同步。
- **API 契约移除**：`GET/POST/DELETE /api/admin/pricing` 已不存在；请求日志的 JSON 不再含 `cost_micros` / `cost_currency` / `pricing_rule_id` 字段（数据库列仍在，但查询不再 SELECT）。
- **`go test -race` 本机可用**：清单中"缺 gcc、race 跑不了"的历史限制**已不成立**，gcc 在 `/c/Strawberry/c/bin/gcc`，本轮全包 race 通过。该限制记录在文中多处出现，均应视为过期。
- 迁移包导出 scope 勾选框从 5 个降为 4 个，`tests/config-migration.test.mjs` 的 scope 断言已同步。
- `tests/log-retry-failover.test.mjs` 与 `tests/logs-filters.test.mjs` 此前会 `extractFunction(logs, "formatLogCostText")` 只为凑齐上下文，已去掉这 6 处提取。
